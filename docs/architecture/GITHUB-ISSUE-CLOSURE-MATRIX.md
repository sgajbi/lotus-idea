# GitHub Issue Closure Matrix

This matrix is the PR-preparation control for the current RFC-0002 issue
hardening branch. It records local closure evidence only. Issues are not
closed until the branch merges, required GitHub checks are green, wiki source is
published from `main` when needed, and branch hygiene is complete.

## Actionable Issue Matrix

| Issue | Status | Implementation Evidence | Test And Gate Evidence | Same-Pattern Scan | Docs/Wiki/Context | PR Close Intent |
| --- | --- | --- | --- | --- | --- | --- |
| [#301](https://github.com/sgajbi/lotus-idea/issues/301) Require explicit signal-evaluation capability | `merged_main` | `src/app/api/signal_api_support.py`, `src/app/security/caller_context.py`, and protected read-route policy usage require role plus `idea.signal.evaluate`. | `tests/unit/test_signal_api_support.py`, `tests/integration/test_high_cash_signal_api.py`, `make signal-api-contract-gate`, `make caller-context-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan hardened advisor queue and candidate-detail read policies so role membership cannot replace `idea.*` capability. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/rfcs/README.md`, `docs/operations/api-certification.md`, `wiki/API-Surface.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#302](https://github.com/sgajbi/lotus-idea/issues/302) Publish caller-context security requirements in OpenAPI | `merged_main` | `src/app/api/caller_context_openapi.py`, `src/app/main.py`, and endpoint certification metadata publish `LotusCallerContext` security and `x-lotus-caller-context`. | `tests/unit/test_caller_context_openapi.py`, `make endpoint-certification-gate`, `make openapi-gate`, `make caller-context-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan covers signal evaluation, outbox run-once, implementation proof, advisor queue, and readiness protected operations. | `docs/operations/api-certification.md`, `wiki/API-Surface.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#303](https://github.com/sgajbi/lotus-idea/issues/303) Add bounded PostgreSQL lookup for downstream submission idempotency records | `merged_main` | `src/app/infrastructure/postgres_repository.py` and `src/app/infrastructure/postgres_downstream_lookup.py` use direct `idea_downstream_submission` primary-key lookup. | `tests/unit/test_postgres_downstream_lookup.py`, `tests/unit/test_downstream_realization.py`, `make test-unit`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan extended bounded repository projections to conversion/report lookup, downstream readiness counts, and runtime trust telemetry. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/rfcs/README.md`, `wiki/Validation-and-CI.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#306](https://github.com/sgajbi/lotus-idea/issues/306) Reject dirty-tree aggregate proof artifacts | `merged_main` | `src/app/application/implementation_proof_readiness.py` requires aggregate proof provenance with `sourceTreeDirty=false`. | `tests/unit/test_implementation_proof_readiness.py`, `make implementation-truth-gate`, `make supported-features-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan keeps dirty or missing provenance from clearing source, Workbench, downstream, mesh, or supported-feature blockers. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/operations/implementation-proof-readiness.md`, `wiki/Validation-and-CI.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#307](https://github.com/sgajbi/lotus-idea/issues/307) Return degraded readiness when configured PostgreSQL cannot initialize | `merged_main` | `src/app/runtime/repository_state.py` and readiness runtime wiring return `503 degraded` with `durable_repository_unavailable`. | `tests/integration/test_health.py`, `tests/unit/test_repository_state.py`, `make test-unit`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan keeps DSNs, driver messages, hostnames, credentials, and raw connection errors out of readiness responses. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/runbooks/service-operations.md`, `wiki/Operations-Runbook.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#308](https://github.com/sgajbi/lotus-idea/issues/308) Align downstream submission OpenAPI errors with runtime ProblemDetails codes | `merged_main` | `src/app/api/problem_details.py`, `src/app/api/signal_api_support.py`, and route metadata publish shared `application/json` plus `application/problem+json` examples. | `tests/unit/test_api_problem_details.py`, `tests/unit/test_signal_api_contract_gate.py`, `make openapi-problem-details-example-gate`, `make openapi-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan moved signal-family and high-cash persist metadata to shared constructors to prevent media-type drift. | `docs/architecture/CODEBASE-REVIEW-LEDGER.md`, `docs/operations/api-certification.md`, `wiki/API-Surface.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#309](https://github.com/sgajbi/lotus-idea/issues/309) Promote duplicate implementation inventory to a blocking CI regression gate | `merged_main` | `scripts/duplicate_implementation_inventory.py`, `Makefile`, and `scripts/ci_contract_gate.py` preserve report-only inventory and blocking strict gate. | `tests/unit/test_duplicate_implementation_inventory.py`, `tests/unit/test_ci_enforcement_contract.py`, `make duplicate-implementation-gate`, `make ci-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan consolidated repeated proof, contract, API, telemetry, request-builder, and source-safe helper bodies before enforcing zero clusters. | `quality/ci_quality_gates.md`, `quality/quality_scorecard.md`, `wiki/Validation-and-CI.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#310](https://github.com/sgajbi/lotus-idea/issues/310) Keep Docker build context in parity with generated-artifact hygiene | `merged_main` | `.dockerignore`, `.gitignore`, `scripts/clean_generated_artifacts.py`, and `scripts/ci_contract_gate.py` align local artifacts outside Docker context; `Dockerfile` and the scheduled-worker contract now also verify that the explicitly governed manifest and imported worker helper scripts are present in the image context and image. | `tests/unit/test_ci_enforcement_contract.py`, `tests/unit/test_source_ingestion_scheduled_worker_contract_gate.py`, `make repository-hygiene-gate`, `make ci-contract-gate`, `make source-ingestion-scheduled-worker-check`, container build and worker `--check-only` smoke. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan covers generated artifacts plus every Compose-declared scheduled-worker runtime asset, preventing a local-pass/container-fail packaging gap. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Validation-and-CI.md`, `wiki/Operations-Runbook.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#311](https://github.com/sgajbi/lotus-idea/issues/311) Align operation metric source-authority vocabulary | `merged_main` | `src/app/observability/logging.py`, `scripts/operation_metric_contract_gate.py`, and operations contract validators share governed source-authority vocabulary. | `tests/unit/test_observability_logging.py`, `tests/unit/test_operation_metric_contract_gate.py`, `make operation-metric-contract-gate`, `make operator-workflows-ops-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan covers runtime operation events, metric contracts, dashboard proof, alert proof, and operator workflow operations contract validation. | `docs/operations/observability.md`, `wiki/Operations-Runbook.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#312](https://github.com/sgajbi/lotus-idea/issues/312) Close source-ingestion Core clients after run-once API execution | `merged_main` | `src/app/api/source_ingestion_readiness.py` closes route-owned `SourceIngestionRuntime` after accepted and source-unavailable executions. | `tests/integration/test_source_ingestion_readiness_api.py`, `tests/unit/test_source_ingestion_state.py`, `make source-ingestion-worker-check`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan applied route-owned cleanup lifecycle to outbox-delivery run-once publisher ownership. | `docs/operations/source-ingestion-run-once.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Operations-Runbook.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#313](https://github.com/sgajbi/lotus-idea/issues/313) Cap source-ingestion run-once manifests | `merged_main` | `src/app/application/source_ingestion.py`, `src/app/application/source_ingestion_worker.py`, and runtime manifest loading enforce the 100-item service ceiling. | `tests/unit/test_source_ingestion.py`, `tests/unit/test_source_ingestion_worker.py`, `tests/unit/test_source_ingestion_state.py`, `tests/integration/test_source_ingestion_readiness_api.py`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan keeps operator run-once and scheduled-worker manifest paths under the same batch ceiling before Core calls or repository mutation. | `docs/operations/source-ingestion-run-once.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Operations-Runbook.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#314](https://github.com/sgajbi/lotus-idea/issues/314) Make route-owned cleanup failures product-safe | `merged_main` | `src/app/api/source_ingestion_readiness.py` and `src/app/api/outbox/delivery.py` isolate cleanup failures into source-safe suppressed operation events. | `tests/integration/test_source_ingestion_readiness_api.py`, `tests/integration/outbox/test_delivery_readiness_api.py`, `make source-observability-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan covers source-ingestion runtime cleanup and outbox publisher cleanup without masking completed, replayed, conflict, or bounded blocked outcomes. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/rfcs/README.md`, `wiki/Operations-Runbook.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#315](https://github.com/sgajbi/lotus-idea/issues/315) Validate signal source refs against owning source authority | `merged_main` | `src/app/api/signal_api_support.py`, `src/app/api/idea_signals.py`, and source-contract helpers validate route-owned source system and product id before domain evaluation. | `tests/integration/test_high_cash_signal_api.py`, `tests/integration/test_high_volatility_signal_api.py`, `tests/integration/test_drawdown_review_signal_api.py`, `tests/integration/test_missing_risk_profile_signal_api.py`, `make signal-api-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan covers Core, Risk, Performance, Manage, and Advise signal families so caller-supplied source refs cannot switch authority labels. | `docs/operations/api-certification.md`, `wiki/API-Surface.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#316](https://github.com/sgajbi/lotus-idea/issues/316) Use repository-side aggregate for review queue readiness | `merged_main` | `src/app/application/review_queue.py`, `src/app/ports/idea_repository.py`, `src/app/infrastructure/postgres_review_queue.py`, and `src/app/infrastructure/postgres_repository.py` use `ReviewQueueReadinessProjectionRepository`. | `tests/unit/test_review_queue_application.py`, `tests/unit/test_postgres_review_queue.py`, `tests/integration/test_review_queue_api.py`, `make endpoint-certification-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan extends bounded aggregate-read posture across outbox readiness, downstream readiness, and runtime trust telemetry projections. | `docs/rfcs/README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Validation-and-CI.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#317](https://github.com/sgajbi/lotus-idea/issues/317) Bound PostgreSQL review and conversion idempotency prechecks | `merged_main` | `src/app/infrastructure/postgres_idempotency_lookup.py` and `src/app/infrastructure/postgres_repository.py` use bounded idempotency-row lookup plus candidate-detail projection. | `tests/unit/test_postgres_idempotency_precheck.py`, `tests/unit/test_review_workflow_application.py`, `tests/unit/test_conversion_workflow_application.py`, `make test-unit`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan applies the same bounded precheck posture to review actions, feedback, conversion intents, and downstream submission idempotency. | `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/rfcs/README.md`, `wiki/Validation-and-CI.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#318](https://github.com/sgajbi/lotus-idea/issues/318) Enforce caller entitlement scope on review and feedback mutations | `merged_main` | `src/app/api/review_workflow.py`, `src/app/api/review_workflow_operations.py`, and review governance mapping bind mutations to trusted caller entitlement headers and persisted candidate scope. | `tests/integration/test_review_workflow_api.py`, `tests/unit/test_review_workflow_api_operations.py`, `tests/unit/test_security_caller_context.py`, `make caller-context-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan checks review actions, feedback, conversion/report candidate-scoped paths, candidate detail, and advisor queue entitlement behavior. | `docs/operations/api-certification.md`, `wiki/API-Surface.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#319](https://github.com/sgajbi/lotus-idea/issues/319) Allowlist governed AI explanation workflow-pack identity | `merged_main` | `src/app/domain/ai_governance.py`, `src/app/application/ai_governance.py`, and `src/app/api/ai_governance.py` reject unregistered workflow pack id/version/evaluation ref before lineage persistence. | `tests/unit/test_ai_governance.py`, `tests/integration/test_ai_governance_api.py`, `tests/unit/test_ai_workflow_pack_registration_proof.py`, `make ai-workflow-pack-registration-proof-contract-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan reconciles API request identity with proof identity and keeps provider runtime execution, RAG, and model operations outside `lotus-idea`. | `docs/rfcs/README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#320](https://github.com/sgajbi/lotus-idea/issues/320) Count only implemented supported-feature entries | `merged_main` | `supported-features/supported-features.schema.json`, `scripts/supported_features_gate.py`, and `src/app/application/implementation_proof_readiness.py` reserve `features[]` for implemented entries only. | `tests/unit/test_supported_feature_readiness.py`, `tests/unit/test_supported_features_gate.py`, `tests/unit/test_implementation_proof_readiness.py`, `make supported-features-gate`, `make implementation-truth-gate`. PR `#321` merged by rebase; Main Releasability `28725934586` and CodeQL `28725932864` passed on `03a2a330`. | same-pattern scan keeps planned and not-applicable posture in `planned_capabilities[]` and prevents those records from clearing readiness blockers. | `docs/operations/supported-feature-promotion.md`, `supported-features/supported-features.json`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Supported-Features.md`. Wiki publication `e3ecce4` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#321`. |
| [#326](https://github.com/sgajbi/lotus-idea/issues/326) Make conversion-outcome ingestion idempotent and state-aware per intent | `merged_main` | `src/app/domain/conversion_outcome_policy.py`, the conversion use case/port, provider enforcement, migration `006`, quarantine-aware posture/readiness, and typed API conflicts preserve source identity/version/correction semantics without a runtime split. | lifecycle/application/API tests, fake-provider parity, real PostgreSQL identity/version races and legacy migration proof, `make conversion-outcome-contract-gate`, migration gates, `make openapi-gate`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan hardens the shared replay-key reservation winner check, current-posture derivation, readiness counting, candidate detail, audit/outbox parity, legacy contradiction handling, and named OpenAPI examples. | `docs/architecture/conversion-outcome-identity-and-lifecycle.md`, RFC-0002 Slices 6/12, API/persistence docs, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`, `wiki/API-Surface.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#327](https://github.com/sgajbi/lotus-idea/issues/327) Protect review and feedback event identity beyond the HTTP idempotency key | `merged_main` | `src/app/domain/review_governance.py`, application prechecks, in-memory enforcement, direct PostgreSQL identity lookup, claim-before-candidate delta ordering, and typed collision retry preserve `reviewId`/`feedbackId` identity without a runtime split. | `tests/unit/test_postgres_review_identity.py`, API suite, real two-connection proof, `make review-identity-contract-gate`, `make openapi-gate`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan covers review and feedback identity fields, terminal review replay, new-key reservation, candidate/audit/outbox parity, API telemetry, OpenAPI examples, and existing primary-key schema authority. | `docs/architecture/review-feedback-identity-and-idempotency.md`, RFC-0002 Slices 6/8, API certification, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`, `wiki/API-Surface.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#328](https://github.com/sgajbi/lotus-idea/issues/328) Propagate and distinguish correlation, trace, and causation metadata in Idea outbox events | `merged_main` | `src/app/api/event_lineage.py`, typed application commands, repository ports/adapters, migration `007`, and the publisher preserve required correlation/trace plus optional parent causation across all seven event families without a runtime split. | domain/mapper/publisher/API tests, real PostgreSQL migration/reload/replay/all-family proof, `make outbox-event-contract-gate`, `make outbox-consumer-contract-gate`, migration and OpenAPI gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan covers every outbox-producing mutation, deterministic system lineage, legacy sanitization, transport headers, replay stability, persistence constraints, producer examples, and consumer replay policy. | `docs/architecture/outbox-event-lineage.md`, RFC-0002 Slice 6, persistence operations, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`, `wiki/API-Surface.md`, `wiki/Troubleshooting.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#329](https://github.com/sgajbi/lotus-idea/issues/329) Expose and alert on actual outbox backlog, lease, and dead-letter posture | `merged_main` | `src/app/observability/outbox/supportability.py`, `src/app/application/outbox/supportability_alerts.py`, and `contracts/observability/lotus-idea-outbox-supportability.v1.json` expose fixed-label state, age, configuration, and collection posture without a runtime split. | projection/collector/threshold tests, contract/proof gates, `make outbox-supportability-rule-test`, full suites, and real PostgreSQL projection proof. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan separates durable state from API traffic and covers collection failure, dead letters, expired leases, backlog count/age, retry pressure, safe labels, dashboard queries, alert timing, and runbook linkage. | RFC-0002 Slices 6/15, observability docs/runbook, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Operations-Runbook.md`, `wiki/Architecture.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#330](https://github.com/sgajbi/lotus-idea/issues/330) Enforce lifecycle and review-posture invariants | `merged_main` | `src/app/domain/candidate_state.py` owns the versioned golden matrix; `IdeaCandidate`, review actions, transition normalization, PostgreSQL codecs/queues, and migration `005` enforce it without a runtime split. | `tests/unit/test_candidate_state_policy.py`, `tests/unit/test_review_governance.py`, `tests/unit/test_postgres_review_queue.py`, `tests/integration/test_review_workflow_api.py`, `make candidate-state-contract-gate`, migration gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan covers construction, JSON rehydration, every review action, repeated commands, queue/readiness SQL, legacy quarantine, OpenAPI conflict examples, audit, and operation telemetry. | `docs/architecture/candidate-lifecycle-review-posture-policy.md`, RFC-0002 Slices 3/6/8, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#331](https://github.com/sgajbi/lotus-idea/issues/331) Reconcile runtime readiness with supported-feature promotion evidence | `merged_main` | `src/app/application/supported_feature_promotion.py`, the CLI gate, readiness application/API, and generated artifact share structured validation and review-freshness truth without a runtime split. | policy/gate/readiness/API/artifact tests, `make supported-features-gate`, `make supported-feature-promotion-contract-gate`, `make implementation-proof-readiness-check`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan removes status-only counting, hard-coded singular projection, missing/stale/path/test/endpoint evidence drift, filesystem leakage, and gate/runtime divergence. | `docs/operations/supported-feature-promotion.md`, RFC-0002 Slices 14/17/19, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Supported-Features.md`, `wiki/Operations-Runbook.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#332](https://github.com/sgajbi/lotus-idea/issues/332) Make advisor queue evaluation time a real as-of or snapshot contract | `merged_main` | `src/app/domain/review_queue/snapshot.py`, the application command, repository ports, in-memory path, PostgreSQL adapter, and API make candidate creation time the queue visibility boundary and bind continuation pages to opaque snapshot identity without a runtime split. | exact-boundary/source-date/token/conflict tests, API integration tests, `tests/integration/test_postgres_review_queue_runtime.py`, `make review-queue-snapshot-contract-gate`, `make openapi-gate`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan covers page counts, readiness aggregates, scope/policy binding, future inserts, backdated inserts, mutation fingerprints, malformed/missing tokens, in-flight adapter changes, and stable ProblemDetails/telemetry codes. | RFC-0002 Slices 7/10/19, API certification ledger, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`, `wiki/Architecture.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#333](https://github.com/sgajbi/lotus-idea/issues/333) Reconcile source evidence dates with signal evaluation as-of dates | `merged_main` | versioned `src/app/domain/source_temporal.py`, every signal-family evaluator, and allocation-drift multi-source checks block exact-date, future-generation, and freshness conflicts before candidate creation/persistence; source corrections preserve lineage and create new identity. | `tests/unit/test_source_temporal.py`, allocation-drift/correction/ingestion tests, adapter API tests, `make source-temporal-contract-gate`, and all-family domain suites. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | same-pattern scan covers every family and caller-supplied, adapter-returned, persisted ingestion, optional cross-domain ref, and source correction path across Core, Risk, Performance, Advise, and Manage. | RFC-0002 Slice 5, API certification, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#334](https://github.com/sgajbi/lotus-idea/issues/334) Make downstream submission state durable across external-call and local-commit failure | `merged_main` | `src/app/domain/downstream_submission.py`, the downstream realization use case/port, in-memory and PostgreSQL adapters, migration `008`, and operator reconciliation APIs enforce claim-before-call, lease-fenced finalization, uncertain posture, and opaque support-reference recovery without granting downstream authority. | Failure-injection, state-machine, adapter, API, OpenAPI, and real PostgreSQL concurrency/restart tests; `make downstream-realization-contract-gate`; migration gates; `make ci` at `99.01%`; disposable PostgreSQL gate with `12 passed`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan hardens outbox re-drive, conversion identity replay, async Performance result polling, supported-feature promotion evidence, malformed persisted audit rejection, and caller-context OpenAPI repair so uncertain work and malformed evidence fail closed across adjacent boundaries. | RFC-0002 Slices 6/12/13, downstream readiness runbook, API certification, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`, `wiki/Architecture.md`, `wiki/Operations-Runbook.md`, `wiki/RFC-Index.md`, `wiki/Roadmap.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#335](https://github.com/sgajbi/lotus-idea/issues/335) Propagate trusted tenant context through Lotus Core source adapters | `merged_main` | `src/app/api/signal_api_support.py`, Core source ports/application commands, `src/app/application/access_scope.py`, `src/app/infrastructure/lotus_core_sources.py`, candidate identity/persistence, worker manifests, and Core live-proof CLIs carry one explicit trusted tenant without a production fallback. | `tests/unit/test_lotus_core_sources.py`, all four Core-backed application test modules, `tests/unit/test_source_ingestion.py`, `tests/integration/test_high_cash_signal_api.py`, `make signal-api-contract-gate`, `make trusted-tenant-context-gate`, and OpenAPI validation. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers every Core-backed signal route, tenant-aware snapshot call, candidate scope/identity, generated ingestion key, scheduled ingestion, certification proof generator, operation-event privacy boundary, and request DTO; missing, ambiguous, untrusted-header, and body-override paths fail before source I/O. | `docs/operations/canonical-opportunity-source-proofs.md`, `docs/operations/source-ingestion-run-once.md`, RFC-0002 Slice 5, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Architecture.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#336](https://github.com/sgajbi/lotus-idea/issues/336) Preserve stable ProblemDetails codes for caller-context boundary failures | `merged_main` | `src/app/errors.py`, `src/app/api/problem_details.py`, `src/app/api/caller_headers.py`, `src/app/api/caller_context_openapi.py`, `src/app/main.py`, and route-local scope consumers preserve typed caller-context status/code/title/detail/category, serve RFC 7807 runtime media, and retain the generic fallback for unrelated framework errors. | `tests/integration/test_caller_context_boundary_api.py`, caller-context security/OpenAPI/ProblemDetails unit tests, `make caller-context-contract-gate`, `make openapi-gate`, `make endpoint-certification-gate`, and `make api-problem-details-boundary-gate`. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers every protected operation plus representative signal, lifecycle, review, AI, report, downstream, and implementation-readiness routes; it proves both OpenAPI media examples, runtime `application/problem+json`, sanitized correlation, bounded diagnostic categories, exact shared details, and absence of raw token/header/scope values. | RFC-0002 Slice 5, `docs/runbooks/service-operations.md`, `docs/operations/api-certification.md`, `src/app/api/README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#337](https://github.com/sgajbi/lotus-idea/issues/337) Add governed dead-letter inspection and re-drive recovery | `merged_main` | `src/app/domain/outbox/recovery.py`, `src/app/application/outbox/recovery.py`, `src/app/ports/idea_repository.py`, PostgreSQL/in-memory adapters, migration `004`, and operator API routes implement source-safe inspection plus one fenced, audited re-drive. PostgreSQL uses an indexed exact opaque-reference selector rather than a fixed-window scan. | Domain/application/adapter/API tests, `tests/integration/test_postgres_runtime_integration.py`, `make outbox-recovery-contract-gate`, migration apply/rollback, OpenAPI, endpoint certification, and required real-PostgreSQL proof. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan preserves retry/failure history for ordinary retry and recovery publication, removes the 1,000-row reachability/locking limit, qualifies the shared delivery `RETURNING` projection, proves restart replay, and applies the shared publisher exception posture without adding a runtime service. | `docs/operations/outbox-dead-letter-recovery.md`, `docs/operations/persistence.md`, RFC-0002 Slice 6, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`, `wiki/Operations-Runbook.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#338](https://github.com/sgajbi/lotus-idea/issues/338) Block unsafe instructions hidden in allowed AI action labels | `merged_main` | `src/app/domain/ai_action_policy.py` applies a versioned deterministic content policy before verification, returns canonical server-owned labels, and retains bounded reasons only. | `tests/unit/test_ai_action_policy.py`, AI domain/API/persistence tests, `make ai-model-risk-ops-contract-gate`, OpenAPI and endpoint gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers allowed action types, normalized obfuscation, unsupported scripts, audit, lineage, replay/conflict, readiness, and model-risk contract drift. | `docs/operations/ai-governance.md`, RFC-0002 Slices 9/15, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Security-and-Governance.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#339](https://github.com/sgajbi/lotus-idea/issues/339) Include redacted AI output content integrity in durable lineage | `merged_main` | `src/app/domain/ai_output_integrity.py`, lineage persistence/codecs, and migration `010` bind ordered advisor-visible output and policy content to a source-safe digest/version with explicit pre-v1 posture. | `tests/unit/test_ai_output_integrity.py`, `tests/unit/test_ai_lineage_content_integrity.py`, API replay/conflict, PostgreSQL, migration, lifecycle, model-risk, and endpoint gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers every accepted, blocked, and fallback content dimension, order/canonicalization behavior, hydration parity, lineage hash, replay, retention, and pre-v1 migration truth. | `docs/operations/ai-governance.md`, RFC-0002 Slices 9/15, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Security-and-Governance.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#341](https://github.com/sgajbi/lotus-idea/issues/341) Replace AI metadata denylists with a provider-safe allowlisted envelope | `merged_main` | `src/app/domain/ai_metadata_policy.py`, typed API DTO mapping, readiness response, and the model-risk contract enforce `lotus-idea.ai-metadata-envelope.v1` before lookup or persistence. | `tests/unit/test_ai_metadata_policy.py`, AI API/OpenAPI tests, `make ai-model-risk-ops-contract-gate`, endpoint and documentation gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers unknown keys, value-only sensitive content, purpose, count, length, control characters, route execution, lineage retention, readiness, and future provider forwarding. | `docs/operations/ai-governance.md`, RFC-0002 Slices 9/15, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/API-Surface.md`, `wiki/Security-and-Governance.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#342](https://github.com/sgajbi/lotus-idea/issues/342) Bind OCI image and version metadata to the published artifact digest | `merged_main` | `src/app/runtime/release_identity.py`, `Dockerfile`, Main Releasability, and `scripts/release_image_identity_contract.py` separate immutable build identity from the final registry/deployment digest and fail production-like readiness closed. | `tests/unit/test_release_identity.py`, `tests/unit/test_release_image_identity_contract.py`, container smoke and CI mutation tests, `make release-image-identity-contract-gate`, Docker build and OCI-label inspection. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers local/PR/published posture, placeholders, partial/malformed/mismatched bindings, release manifest, Kubernetes reference, signature/provenance/SBOM subjects, exact digest runtime, promotion, and secret build inputs. | `docs/runbooks/service-operations.md`, RFC-0002 Slices 15/19, `README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Validation-and-CI.md`. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#343](https://github.com/sgajbi/lotus-idea/issues/343) Define and prove Lotus Idea PostgreSQL backup and disaster recovery readiness | `partially_fixed` | `contracts/operations/lotus-idea-postgres-disaster-recovery.v1.json`, layered recovery policy/use case/port/PostgreSQL inspector, logical and provider-restore adapters, 17-table representative fixture, resume validator, recovery-aware readiness/write guard, and weekly attested workflow implement the repo-owned DR boundary without taking provider ownership. | Main dispatch `29157824527` at `e565c915` passed with PostgreSQL 18.4, all 17 tables through migration `012`, zero invariant failures, RPO `0s`, RTO `0.193s`, replay/fencing/no-mutation proof, provenance, and retained evidence; artifact posture remains `not_certified` and `pitrProof=false`. | Same-pattern scan corrected client/server version drift, absent PGDG package authority, suppressed source-safe diagnostics, missing explicit restore target, row-hash SQL aliasing, direct-script import posture, replay/fencing behavior, evidence-writer duplication, migration-vs-DR claims, and synthetic-vs-PITR boundaries. | `docs/runbooks/postgres-disaster-recovery.md`, RFC-0002 Slice 6 and parent RFC, `README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/PostgreSQL-Disaster-Recovery.md`, `wiki/Operations-Runbook.md`, `wiki/Home.md`. | Keep #343 open until approved provider topology, encrypted backup/WAL health, physical/PITR exercise, failover/cutover proof, and production authorization evidence satisfy the issue acceptance criteria; no supported-feature promotion. |
| [#344](https://github.com/sgajbi/lotus-idea/issues/344) Define retention, legal-hold, and erasure lifecycle for Idea-owned records | `partially_fixed` | `contracts/operations/lotus-archive-lifecycle-posture-consumer.v1.json`, `src/app/application/data_lifecycle/archive_posture_verification.py`, `src/app/domain/data_lifecycle/archive_posture.py`, `src/app/infrastructure/data_lifecycle/postgres_policy.py`, and migration `015` implement the signed Archive posture consumer without taking external authority. Exact linked report evidence is verified, bounded lineage persists, and applied decision/digest replay is fenced. | Archive verification/policy/API tests, mutation-tested `make archive-lifecycle-posture-contract-gate`, migration apply/rollback, OpenAPI, and required PostgreSQL 18 runtime proof cover strict trust, exact linkage, hold/purge semantics, persistence, restart replay, and cross-key conflict. | Same-pattern scans cover independent authority, strict DTOs, source-safe failures, exact transactional linkage, blocked-attempt non-consumption, trust-key status/window, TTL, digest/signature, and durable replay. No runtime service split was justified. | Lifecycle runbook, RFC-0002 Slice 6/index, `README.md`, repository context, and lifecycle/integration/security wiki pages. Supported features remain unchanged. | Keep #344 open until live bank producer/key-discovery proof, bank policy approval, managed Archive store/key/trust proof, provider-native AI conformance, production authorized purge proof, mainline evidence, and promotion criteria are satisfied. |
| [#346](https://github.com/sgajbi/lotus-idea/issues/346) Add license and IP compliance gates for runtime and CI dependencies | `merged_main` | `contracts/compliance/lotus-idea-license-policy.v1.json`, `scripts/license_compliance_policy.py`, deterministic `THIRD_PARTY_NOTICES.md`, package/image license inclusion, `.github/CODEOWNERS`, and release-manifest binding govern the exact runtime/CI closure without self-asserting legal approval. | `tests/unit/test_license_compliance_gate.py`, CI mutation and release-evidence tests, `make license-compliance-gate`, `make license-release-evidence-gate`, wheel/container inspection, and aggregate repository gates. PR `#347` and fix-forward PR `#348` merged; Main Releasability `29154949303` and CodeQL `29154947568` passed on `ad88690f`. | Same-pattern scan covers direct/transitive and lock drift, unknown/denied/conditional/dual expressions, orphaned/duplicate/expired evidence-backed exceptions, base/scanner images, generated/model/data assets, NOTICE reproducibility, SBOM serial, lock hashes, exception IDs, and digest target parity. | `docs/operations/license-ip-compliance.md`, `SECURITY.md`, RFC-0002 Slices 15/19, `README.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `wiki/Security-and-Governance.md`. Supported features are unchanged. Wiki publication `2d5b9a5` is synchronized from merged main. | Closed on merged `main`; local and remote implementation branches were deleted after PR `#347` and fix-forward PR `#348`. |
| [#357](https://github.com/sgajbi/lotus-idea/issues/357) Introduce governed feature-bounded packages within Idea runtime layers | `merged_main` | The lifecycle pilot plus `src/app/domain/outbox/`, `src/app/application/outbox/`, and corresponding layer packages establish the repeated capability pattern with no compatibility aliases or runtime split. | Focused outbox/domain/hygiene suites, direct generator execution, all outbox contract gates, Ruff, MyPy, architecture, private-import, maintainability, duplicate-implementation, repository-hygiene, disposable PostgreSQL, package/wheel/container smoke, and full `make ci`; PR `#362` merged by rebase and Main Releasability `29235051710` plus CodeQL `29235047521` passed on `eba19925`. | Same-pattern scan moved capability-owned event/lineage, in-memory write, PostgreSQL fake, and integration-proof modules; retained shared implementation-proof consumers with their actual owner; and added semantic required/retired-path enforcement without directory-size limits. | `README.md`, RFC-0002 Slice 6, `REPOSITORY-ENGINEERING-CONTEXT.md`, review/refactor ledgers, and `wiki/Architecture.md`. Wiki publication commit `5534db5` has zero source drift. Supported features are unchanged; existing platform capability-package guidance remains sufficient. | Closed on merged `main`; local and remote feature branches were deleted. Future capability packages require new measured evidence rather than keeping this issue unbounded. |
| [#363](https://github.com/sgajbi/lotus-idea/issues/363) Avoid whole-store snapshot hydration for candidate persistence writes | `merged_main` | `src/app/infrastructure/persistence/aggregate_mutation.py`, the mutation-visible candidate projection, and `PostgresIdeaRepository.persist_candidate` load only requested/idempotency-linked aggregates and acquire ordered candidate/idempotency transaction locks before domain evaluation and atomic delta writes. | `tests/unit/persistence/test_aggregate_mutation.py`, repository regressions, `tests/integration/persistence/test_candidate_persistence_runtime.py`, real PostgreSQL 18, repository-hygiene, architecture, maintainability, duplicate, Ruff, and MyPy gates; PR `#365` merged by rebase and Main Releasability `29239140276` plus CodeQL `29239134509` passed on `69326064`. | The #364 same-pattern completion proves the fix across every ordinary mutation/replay family rather than hiding the broader defect behind candidate-only scope. | RFC-0002 Slice 6/index, `docs/operations/persistence.md`, repository context, review/refactor ledgers, and `wiki/Architecture.md`. Wiki publication commit `8386705` has zero source drift. OpenAPI, migrations, platform skills, and supported features are unchanged. | Closed on merged `main`; local and remote implementation branches were deleted after tree-equivalence proof. |
| [#364](https://github.com/sgajbi/lotus-idea/issues/364) Bound ordinary PostgreSQL mutations and replay to capability state | `merged_main` | `src/app/infrastructure/persistence/aggregate_mutation.py`, `postgres_mutation.py`, and `postgres_replay.py` compose identity-fenced candidate aggregates, idempotency-only outbox-run state, candidate-only replay, and exact report precheck state. AI request and replay-nonce candidate linkage is bounded in `postgres_ai_lineage_identity.py`; ordinary repository paths no longer call full snapshots. | `tests/unit/persistence/test_aggregate_mutation.py`, repository/identity tests, full `make ci`, and all 17 disposable PostgreSQL 18 tests passed. PR `#365` merged by rebase and Main Releasability `29239140276` plus CodeQL `29239134509` passed on `69326064`; the release lane also validated the published SHA image, scan, SBOM, signature, attestations, digest, and runtime identity. | Same-pattern scan covers candidate, lifecycle, review, feedback, conversion intent/outcome, report evidence, AI lineage, outbox run idempotency, evidence replay, and report precheck. Full snapshots remain explicit administrative/test/DR operations only. | `README.md`, RFC-0002 Slice 6/index, `docs/operations/persistence.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, review/refactor ledgers, and `wiki/Architecture.md` are synchronized. Wiki publication commit `8386705` has zero source drift. No API/OpenAPI, migration, database, schema, supported-feature, platform-skill, or runtime-split change is claimed. | Closed on merged `main`; local and remote implementation branches were deleted after tree-equivalence proof. |
| [#372](https://github.com/sgajbi/lotus-idea/issues/372) Make deployment migrations serialized, drift-safe, and release-evidenced | `merged_main` | `contracts/operations/lotus-idea-deployment-migrations.v1.json`, the deployment-migration domain/application/port/PostgreSQL adapter, exact-image CLI, evidence gate, Docker closure, and protected workflow implement the complete repository-owned deployment boundary. | Focused migration and CI suites, eight real PostgreSQL 18 cases, and full `make ci` pass. PR `#373` merged by rebase at `6ba9618a`; Main Releasability `29261043056` and CodeQL `29261035371` passed for that exact SHA. The release manifest binds digest `sha256:24cff4c4528f6d06eba2f87e2d151f104e6e2303785ccfcfef589511f0ad46b7` to the full-SHA tag, OCI labels, matching `/version`, zero-finding vulnerability/secret scan, CycloneDX SBOM, Cosign signature, provenance/SBOM attestations, Kubernetes digest reference, and same-image promotion policy. | Same-pattern scan classifies direct migration execution in scheduled lifecycle review and DR seeding as approved disposable fixtures, rejects future workflow bypass, verifies secret-free runtime injection, exact-image reuse, Docker asset closure, migration/history drift, concurrent plans, legacy adoption, rollback, and evidence tampering. | `README.md`, RFC-0002 Slice 6/index, `docs/operations/persistence.md`, migration standard, `REPOSITORY-ENGINEERING-CONTEXT.md`, review/issue ledgers, and Architecture/Operations/Validation/Roadmap/Supported-Features wiki pages. Wiki publication `bda1965` has zero source drift. Registry remains `foundation_only`; no platform skill change is needed because existing backend/CI governance already requires full operator-asset closure and exact-image evidence. | Closed on merged `main` after exact-main checks and wiki publication; the local and remote implementation branch was deleted. Protected environment execution, approved production change, and rollout-health attestation remain explicit certification blockers and do not claim production certification. |
| [#381](https://github.com/sgajbi/lotus-idea/issues/381) Make standalone PostgreSQL startup and migrations restart-safe | `merged_main` | `docker-compose.yml` owns PostgreSQL 18, a major-version-correct named volume, one-shot migrations, and shared API/worker durable wiring. `src/app/infrastructure/migrations.py` adds advisory-locked pending-only local history with checksum drift and rollback tracking; production deployment migration controls remain unchanged. | `tests/unit/test_migration_execution.py` and `tests/unit/test_ci_release_evidence_contract.py` cover restart/drift/rollback and Compose/Dockerfile mutation. Real restart/readback proof passed; PR `#383`, CodeQL `29290922521`, and Main Releasability `29291215703` passed on exact main SHA `4f4e0985`. | Same-pattern scan covered PostgreSQL major-version volume layout, app/worker database parity, migration asset closure, repeated startup, in-memory boundary truth, canonical Workbench orchestration, script package organization, and platform backend/runtime skill guidance. | `README.md`, persistence/operations docs, Slice 06, repository context, and `wiki/Operations-Runbook.md` are synchronized. Governed wiki publication follows closure merge; supported features are unchanged and local Compose proof is non-certifying. | Closed by merged PR `#383`; exact-main validation passed and branch/wiki hygiene is completed by the governed closure flow. |
| [#382](https://github.com/sgajbi/lotus-idea/issues/382) Separate candidate score policy from review-queue ranking policy | `merged_main` | `src/app/domain/scoring.py` owns candidate score provenance; `src/app/domain/review_queue/policy.py` owns comparability, priority, exclusions, and ordering; `src/app/domain/review_queue/snapshot.py`, application ports/use case, PostgreSQL adapter, readiness, and API preserve the distinction end to end. | `tests/unit/test_candidate_score_policy_registry.py`, `tests/unit/test_scoring_queue_policy.py`, `tests/unit/test_review_queue_application.py`, `tests/unit/test_postgres_review_queue.py`, snapshot mutation, API/OpenAPI, repository-hygiene, and contract-gate tests prove unknown-policy exclusion, provider parity, policy-set snapshot binding, readiness blocking, and response semantics. PR `#383`, CodeQL `29290922521`, and Main Releasability `29291215703` passed on exact main SHA `4f4e0985`. | Same-pattern scan covered all signal-family defaults, process-local and SQL eligibility, readiness aggregates, snapshot identity, API examples/descriptions, imports, flat-domain organization, and runtime-split pressure. | Slice 07/index, `README.md`, repository context, endpoint ledger, review ledger, and Architecture/API/RFC wiki pages are synchronized. Governed wiki publication follows closure merge; supported features remain unchanged. | Closed by merged PR `#383`; exact-main validation passed and branch/wiki hygiene is completed by the governed closure flow. |
| [#385](https://github.com/sgajbi/lotus-idea/issues/385) Route Idea candidates through audience-specific review queues | `merged_main` | `src/app/domain/review_queue/`, `src/app/application/review_queue.py`, the repository port and PostgreSQL adapter, and `src/app/api/review_queue/` bind advisor, portfolio-manager, and compliance audience through eligibility, readiness, snapshot identity, API routing, and OpenAPI. `src/app/application/review_queue_exceptions.py` provides an aggregate-only operator exception posture without business authority or a runtime split. | `tests/unit/test_review_queue_application.py`, `tests/unit/test_review_queue_policy.py`, `tests/unit/test_review_queue_snapshot.py`, `tests/unit/test_postgres_review_queue.py`, and `tests/integration/test_review_queue_api.py` prove role isolation, audience filtering, snapshot conflict, provider readiness parity, and candidate-safe operator output. PR Merge Gate `29297546881`, exact-main Main Releasability `29297787754`, and CodeQL `29297783153` passed. | The same-pattern scan found and fixed audience-neutral process-local readiness, verified PostgreSQL predicates, bound audience into continuation identity, separated operator support posture from business ranking, and grouped API files by capability rather than leaving another flat route module. | `README.md`, `docs/rfcs/`, `docs/operations/api-certification.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/architecture/CODEBASE-REVIEW-LEDGER.md`, and repo-authored `wiki/` pages are synchronized. Wiki publication commit `3ad971d` passed the zero-drift check. Supported features remain unchanged. | Issue `#385` closed after PR `#387` merged to exact-main SHA `d5be2390065171a36fc8fbc19f40cc4aa3cded87`, exact-main QA passed, and local/remote implementation branch cleanup completed. |
| [#386](https://github.com/sgajbi/lotus-idea/issues/386) Derive review mutation scope from trusted caller and persisted candidate truth | `merged_main` | `src/app/api/review_workflow.py`, `src/app/api/review_workflow_operations.py`, and `src/app/application/review_workflow.py` map actor entitlements only from trusted caller context, load scope from the persisted candidate, and keep caller-supplied scope claims out of request and domain command contracts. | `tests/unit/test_review_governance.py`, `tests/unit/test_review_workflow_application.py`, and `tests/integration/test_review_workflow_api.py` cover every scope dimension, missing and partial headers, multi-value membership, legacy body-field rejection, product-safe denial, replay, and OpenAPI truth. PR Merge Gate `29297546881`, exact-main Main Releasability `29297787754`, and CodeQL `29297783153` passed. | The same-pattern scan removed both `accessScope` and `authorizedScope`, eliminated nullable placeholder scope from domain commands, verified review and feedback parity, and added reusable Lotus backend skill guidance for persisted-resource authorization. | `docs/rfcs/`, `docs/operations/api-certification.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/architecture/CODEBASE-REVIEW-LEDGER.md`, and repo-authored `wiki/` pages are synchronized. Wiki publication commit `3ad971d` passed the zero-drift check. Supported features remain unchanged. | Issue `#386` closed after PR `#387` merged to exact-main SHA `d5be2390065171a36fc8fbc19f40cc4aa3cded87`, exact-main QA passed, and local/remote implementation branch cleanup completed. |
| [#389](https://github.com/sgajbi/lotus-idea/issues/389) Bind advisor-visible AI narratives to verified evidence claims | `merged_main` | `src/app/domain/ai_explanation/grounding.py`, `src/app/domain/ai_governance.py`, API response projection, readiness, output integrity, and the model-risk contract replace accepted provider narrative with a deterministic projection of verified ordered claims and source-safe references. | `tests/unit/test_ai_claim_grounding.py`, `tests/unit/test_ai_governance.py`, and `tests/integration/test_ai_governance_api.py` prove laundering resistance, exact grounding, blocked-output omission, duplicate-identity rejection, replay, and policy posture. PR `#390` merged by rebase; Main Releasability `29300549721` and CodeQL `29300546423` passed on exact main SHA `67a6e005`. | The same-pattern scan covered accepted, unsupported-claim, forbidden-action, fallback, response, output-integrity, lineage, replay, and readiness paths; submitted narrative remains digest-bound but absent from response and persistence. | `docs/research/advisor-intelligence-product-differentiation.md`, Slice 09/index, `docs/operations/ai-governance.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, review ledger, and `wiki/` Architecture/API/RFC pages are synchronized. Wiki publication `f86a57c` passed with zero drift; supported features remain unchanged. | Closed after PR `#390`, exact-main CI, wiki publication, and implementation-branch cleanup. |
| [#392](https://github.com/sgajbi/lotus-idea/issues/392) Require actual execution evidence before clearing the Lotus AI runtime blocker | `locally_fixed` | `src/app/application/ai_runtime_proof/`, `src/app/ports/lotus_ai_runtime.py`, `src/app/infrastructure/lotus_ai/`, and `scripts/generate_ai_workflow_pack_runtime_execution_proof.py` replace source-text inference with an actual governed Lotus AI execution and bounded receipt. | `tests/unit/test_ai_workflow_pack_runtime_execution_proof.py`, `tests/unit/test_lotus_ai_workflow_runtime.py`, the aggregate-readiness tests, and `make ai-workflow-pack-runtime-execution-proof-contract-gate` pass. A canonical Lotus AI service on `8140` produced a completed deterministic-stub run with `ACTION_REQUIRED` review posture and blocked publication/authority. | The same-pattern scan found evidence-class ambiguity in other proof builders and raised #393 with concrete outbox, persistence, and Gateway/Workbench examples. | `docs/rfcs/`, `docs/operations/implementation-proof-readiness.md`, `REPOSITORY-ENGINEERING-CONTEXT.md`, `docs/architecture/CODEBASE-REVIEW-LEDGER.md`, demo claims, and authored `wiki/` validation/RFC/demo/support pages are synchronized. Supported features remain unchanged. | `Closes #392` after PR merge, exact-main CI, wiki publication, and branch cleanup. Live-provider/production approval remains #340. |

## Closure Rules

Slice 06 AI conformance now additionally verifies and persists
`lotus-ai:ProviderRetentionConfirmation:v1` through migration `014`, with
tenant/run/provider/model binding, confirmation/reference/nonce replay fencing,
source-safe lineage hashing, and explicit non-authority semantics. Focused
verification, API, codec, migration, replay, and cross-tenant no-write tests
pass. Issue #344 remains `partially_fixed`: provider-native evidence,
managed-key/production-SQL proof, bank approvals, Report/Archive production
conformance, authorized purge, and mainline proof remain open.

1. `#225` remains the campaign ledger and is intentionally excluded from the
   actionable closure set.
2. A row can be `locally_fixed` only when implementation, meaningful tests,
   same-pattern scan, docs/wiki/context decision, and PR close intent are
   present on the branch.
3. A row can be `merged_main` only after it records Main Releasability and
   CodeQL evidence on the merged commit, wiki publication, issue closure, and
   local and remote branch cleanup.
4. The PR description should use the `PR Close Intent` column so GitHub closes
   the issue only after merge.
5. The matrix is guarded by `make github-issue-closure-matrix-gate` and by
   `make lint`.
6. `MERGED_MAIN_ISSUES` in the gate freezes completed issue state so a later
   documentation edit cannot silently regress merged truth to branch-local
   wording. Closure reconciliation must also remove active-work GitHub labels
   such as `status/pr-open`, `status/fixed-local`, and `status/in-progress`.
