# Codebase Review Ledger

This ledger records implementation-backed cleanup and modularity slices for
lotus-idea. It is not an aspirational backlog; entries must carry evidence or
remain explicitly open.

## Current Slice Follow-Up

| Review ID | Scope / Pattern | Status | Finding | Action Taken | Evidence | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| `LI-CR-0063` | Source-ingestion runtime evidence authority and capability ownership | `Hardened locally; mainline validation pending` | Issue `#456` found that v1 cleared a live-Core blocker from self-asserted attempt/status/durability/count fields, accepted unknown claims, and discarded the authoritative per-item use-case/persistence results. Flat implementation, automation, and tests obscured ownership. | Commit `1be6788f` replaces v1 with a closed-field v2 `runtime_execution` contract. It consumes the actual application result, requires exact current Core products, accepted/replayed-only decisions, durable persisted-record receipts, count/hash/scope reconciliation, and explicit false non-proof claims. Implementation, automation, and focused tests are capability-owned; six retired flat paths are prohibited. The declarative hygiene policy now lives in `scripts/repository_hygiene/policy.py`, reducing the executable gate from 511 to 120 lines without changing its CLI. Runtime evidence consumers share the domain-owned timezone-aware parser instead of duplicating proof-provenance parsing. | Ruff, MyPy over 830 files, direct contract and CI/repository-hygiene gates, 149 focused domain/application/API/aggregate tests, 21 hygiene behavior tests, 103 focused evidence tests, the maintainability gate, and the zero-cluster duplicate-implementation gate pass. The adversarial matrix rejects unknown fields, authority/freshness/hash/scope/count drift, in-memory posture, mixed outcomes, and production/promotion claim inflation. | Design modularity only; the existing API and worker remain one deployable over one Idea-owned database. OpenAPI shape, migrations, database ownership, and supported-feature posture are unchanged. PR, exact-main CI, wiki publication, issue closure, and branch cleanup remain required. |
| `LI-CR-0062` | Runtime trust telemetry evidence class and capability ownership | `Hardened on main` | Issue `#452` found that a deterministic in-memory fixture was described as runtime certification proof, cleared `runtime_candidate_snapshot_missing`, and reported `runtimeTelemetryBacked=true` even when durable storage was explicitly false. Flat implementation, automation, and tests obscured ownership. | PR `#453` replaced the artifact with a closed-field v2 `test_execution` contract that clears no blocker, preserves durable/runtime/certification boundaries, rejects authority claim inflation, derives the preview runtime-backed flag from actual repository posture, and groups implementation, scripts, and tests under `runtime_trust_telemetry/` with retired paths prohibited. | Focused domain/API, aggregate-readiness, CI-contract, and repository-hygiene suites pass. The mutation matrix rejects unknown fields, identity/count/blocker changes, and forged runtime, API, tenant, deployment, production, certification, or promotion claims. `make check` passed `3,876` unit tests; `make ci` passed `451` integration tests with `28` expected skips, `4` E2E tests, `99.01%` combined coverage, and dependency audit. PR `#453` merged by rebase to exact-main SHA `687ae5bfcb0141707980d4522275a72969fb6165`; PR Merge Gate `29376972102`, Main Releasability `29377188016`, and CodeQL `29377183417` passed. Wiki publication `184ee77` has zero source drift. | Design modularity only; the existing Idea service remains one deployable. No OpenAPI shape, migration, database ownership, runtime topology, or supported-feature promotion is justified. Issue #452 is closed on exact main with `status/merged-main`; the implementation branch is absent locally and remotely. Delete this closure branch after merge and keep #393 open. |
| `LI-CR-0061` | Advise/Manage route evidence class and downstream-realization ownership | `Hardened on main` | Issue `#449` found that sibling files and static JSON contracts cleared `advise_live_contract_proof_missing` and `manage_live_contract_proof_missing` even though the artifacts explicitly recorded that downstream execution was not proven. Flat implementation, script, fixture, and test paths also obscured capability ownership. | PR `#450` replaced the v1 proof atomically with closed-field v2 `source_contract` artifacts; bound each owning repository's contract, route, and service declarations by ref/SHA-256; cleared no blocker; preserved live-route and authority blockers; rejected runtime/authorization/tenant/acceptance/authority/production/promotion claim inflation; consumed valid current artifacts as supporting evidence only; and required capability-owned `downstream_realization/` paths while prohibiting retired flat locations. | Focused coverage passed `131` tests; the final source-contract suite passed `24` tests with `100%` module coverage. `make check` passed MyPy over `824` source files and `3,858` unit tests before the final edge tests. Final `make ci` passed `3,861` unit tests, `451` integration tests with `28` expected skips, `4` E2E tests, `99.00%` combined coverage, and dependency audit. PR `#450` merged by rebase to exact-main SHA `e159c269d96f3d3b570a15fb9e1a6292d6252e02`; Main Releasability `29374109211` and CodeQL `29374104007` passed. Wiki publication `430491a` has zero drift. | Design modularity only; the existing Idea service remains one deployable. API/OpenAPI shape, migrations, database ownership, runtime topology, and supported-feature posture remain unchanged. Issue `#449` is closed on exact main with `status/merged-main`; implementation branches are absent locally and remotely. Delete this closure branch after merge. Keep `#393`, `#340`, and `#380` open. |
| `LI-CR-0060` | Mesh policy evidence class and data-mesh ownership | `Hardened on main` | Issue `#444` found that a v1 artifact built entirely from local readiness, SLO, access, and evidence-policy JSON cleared three certification-named blockers without an evidence class or certifying authority. Its validator accepted unknown claim fields, and flat implementation/script/test paths obscured data-mesh ownership. | PR `#447` replaced it atomically with a closed-field v2 `source_contract`; bound all four policy sources by repository/ref/SHA-256; cleared no blocker; preserved every certification, runtime, activation, discovery, deployment, production, and promotion boundary; consumed valid current artifacts as supporting evidence only; and required capability-owned `data_mesh/` paths while prohibiting retired flat locations. | Focused coverage passed `167` tests. `make check` passed `3,876` unit tests. `make ci` passed MyPy over `822` source files, integration (`451` passed, `28` expected skips), E2E (`4` passed), `99.01%` combined coverage, and dependency audit. PR `#447` merged by rebase to exact-main SHA `d4e7dcec3c6cc686aa93e55afb61154fc493303c`; Main Releasability `29368885180` and CodeQL `29368878920` passed. Wiki publication `d428a88` has zero drift. | Design modularity only; the existing Idea service remains one deployable. OpenAPI, migrations, database ownership, runtime topology, and supported-feature posture remain unchanged. Issue `#444` is closed on exact main with `status/merged-main`; implementation branches are absent locally and remotely. Delete this closure branch after merge. Keep `#393` and `#380` open. |
| `LI-CR-0059` | Platform catalog evidence class and data-mesh ownership | `Hardened on main` | Issue `#443` found that a path-only v1 onboarding artifact used static sibling platform declarations to clear source-manifest and catalog-inclusion blockers without declaring an evidence class. Its open validator accepted claim inflation, ambiguous onboarding vocabulary implied broader operation, and flat application/script/test paths obscured data-mesh ownership. | PR `#445` replaced it atomically with a closed-field v2 `source_contract`; bound the exact platform source manifest, catalog, dependency graph, and maturity matrix by repository/ref/SHA-256; satisfied only the two inclusion blockers; retained all runtime, certification, activation, discovery, deployment, production, and promotion boundaries; updated dependent Workbench discovery consumption; and required capability-owned `data_mesh/` paths while prohibiting retired flat locations. | Focused source-contract, aggregate-readiness, runtime-artifact, and Workbench coverage passed (`163` tests). Repository-hygiene and CI-contract coverage passed (`72` tests). Full `make ci` passed MyPy over `822` source files, `3,860` unit tests, integration (`451` passed, `28` expected skips), E2E (`4` passed), `99.01%` combined coverage, and dependency audit with no known vulnerabilities. PR `#445` merged by rebase to exact-main SHA `85b23d2820160273102dc0f3e55194b21bceb95b`; Main Releasability `29365118262` and CodeQL `29365112353` passed. Wiki publication `43da2ba` has zero drift. | Design modularity only; the existing Idea service remains one deployable. The README data-mesh row is synchronized; OpenAPI, migrations, database ownership, runtime topology, and supported-feature posture remain unchanged. Issue `#443` is closed on exact main with `status/merged-main`; the implementation branch is absent locally and remotely. Issue `#444` tracks the next static-policy occurrence. Keep `#393` and `#380` open. |
| `LI-CR-0058` | Report materialization evidence class and capability ownership | `Hardened on main` | Issue `#438` found that a static sibling Report contract was projected as materialization, render, and archive execution proof, cleared three runtime blockers, and changed the current downstream target without an observed report job, output, archive record, retention/legal-hold posture, deployment, or production evidence. Flat application, script, gate, and test paths also obscured ownership. | Replace the artifact with a closed-field v2 `source_contract` contract that clears no blocker, preserves target/readiness/supportability posture, rejects runtime and authority claim inflation, records valid source-contract refs as supporting evidence, and groups implementation, automation, and focused tests under capability-owned `report/` packages with retired paths prohibited. | Focused source-contract and aggregate-readiness coverage passed (`65` tests); downstream, generator, and CI-contract coverage passed (`89` tests); repository-hygiene coverage passed (`16` tests); direct `make report-materialization-source-contract-proof-gate` passed. `make check` passed MyPy over `822` source files and all `3,844` unit tests. `make ci` passed integration (`451` passed, `28` expected skips), E2E (`4` passed), `99.01%` combined coverage, and dependency audit. PR `#441` merged by rebase to exact-main SHA `9dce803bb5d7ed834b18c5d3ae118d8ff841dfb0`; Main Releasability `29360465408` and CodeQL `29360459620` passed. Wiki publication `5a1ca40` has zero drift. | Design modularity only; the existing Idea service remains one deployable. Report/Render/Archive runtime execution, retention/legal-hold authority, client publication, production certification, and supported-feature evidence remain owning-service/runtime blockers. No OpenAPI shape, migration, database ownership, or supported-feature promotion is justified. Issue #438 is closed on exact main with `status/merged-main`; the implementation branch is absent locally and remotely. Delete this closure branch after merge. |
| `LI-CR-0057` | Report intake route evidence class and capability ownership | `Hardened on main` | Issue `#437` found that a sibling static contract was accepted as live Report intake proof and cleared `lotus_report_live_intake_route_proof_missing` without observing route serving, authorization, tenant isolation, or request execution. Flat application, script, gate, and test paths also obscured ownership. | The v2 `source_contract` artifact adds declaration provenance but clears no blocker, preserves the live-intake blocker, rejects runtime and downstream claim inflation, and groups implementation, automation, and focused tests under capability-owned `report/` packages with retired paths prohibited. | Focused coverage passed `151` tests; `make check` passed `3,835` unit tests; `make ci` passed `451` integration tests with `28` expected skips, `4` E2E tests, `99.01%` coverage, and dependency audit. PR `#439` merged by rebase to exact-main SHA `1a64ef69c70958855302373f4e139513a019570e`; Main Releasability `29356075075` and CodeQL `29356064752` passed. | Design modularity only; the existing Idea service remains one deployable. Report serving, acceptance, materialization, render, archive, publication, and supported-feature evidence remain runtime or owning-service blockers. Wiki publication `4a43d9d` has zero drift; the implementation branch is absent locally and remotely. |
| `LI-CR-0056` | Workbench read-path evidence class and package ownership | `Hardened on main` | Issue `#434` found that local files, Make targets, fixed Gateway route strings, and historical Workbench PR/SHA references were accepted as proof of BFF consumption and cleared `workbench_gateway_bff_consumption_proof_missing` without observing Gateway serving, Workbench rendering/consumption, entitlements, browser behavior, or a canonical journey. Flat builder, script, gate, and test paths also obscured ownership. | The v2 `source_contract` proof clears no blocker, preserves the runtime-consumption blocker, rejects runtime/browser/entitlement/certification/promotion claim inflation, removes fixed PR/SHA claims, and groups implementation, automation, and focused tests under capability-owned `workbench/` packages with retired paths prohibited. | Focused source-contract and dependent readiness/API suites passed `178` tests; CI-contract coverage passed `55` tests; `make check` passed all `3,829` unit tests; `make ci` passed unit, integration (`451` passed, `28` expected skips), E2E (`4` passed), `99%` coverage, and dependency audit. PR `#435` merged by rebase to exact-main SHA `9b7b3eba31e6433d60c2d055f5a404afcc84e460`; Main Releasability `29351807566` and CodeQL `29351801033` passed. | Design modularity only; the existing Idea service remains one deployable. Gateway serving, Workbench consumption, entitlement, browser, publication, canonical runtime, and supported-feature evidence remain external/runtime blockers. Wiki publication `d6cf876` has zero source drift; the implementation branch is absent locally and remotely. |
| `LI-CR-0055` | Immutable issue-closure enforcement | `Hardened on main` | Issue `#424` found that #419 was recorded as `merged_main` in the closure matrix after exact-main CI, wiki publication, issue reconciliation, and branch cleanup, but was absent from the gate's immutable merged-main set. A later documentation edit could therefore regress #419 to a local-only status without failing CI. | PR `#426` added #419 to `MERGED_MAIN_ISSUES` and a focused mutation test proving the gate rejects a regression to `locally_fixed`. It also registered #424 as actionable so its own implementation and closure evidence cannot be omitted from the matrix. | Focused gate and documentation coverage passed (`21` tests); `make github-issue-closure-matrix-gate documentation-contract-gate` passed. `make check` passed MyPy over `819` source files and all `3,813` unit tests. PR `#426` merged by rebase to exact-main SHA `a64b3e8f`; Main Releasability `29339920925` and CodeQL `29339900676` passed. | Governance enforcement only. No runtime topology, API, OpenAPI, migration, database ownership, supported-feature, product-support, authored wiki, or repository-layout change was justified. Issue #424 is closed on exact main with `status/merged-main`; the implementation branch is absent locally and remotely. Delete this closure branch after merge. |
| `LI-CR-0054` | Platform-mesh event proof evidence class and package ownership | `Hardened on main` | Issue `#422` found that static event/consumer declarations plus platform source-manifest and catalog files were described as publication proof and cleared `platform_mesh_event_publication_proof_missing` without contacting a broker, publishing an event, observing a receipt, or executing a consumer. Flat builder, script, and test paths obscured capability ownership. | PR `#423` replaced the artifact with a v2 `source_contract` proof that clears no blocker, preserves the publication runtime blocker, rejects runtime/publication/deployment/production claim inflation, adds provenance without changing readiness, and groups builder, generator, gate, and tests under capability-owned `outbox/platform_mesh/` packages with retired paths prohibited. | Focused coverage passed (`121` tests). `make check` passed MyPy over `819` source files and `3,811` unit tests. `make ci` passed `451` integration tests (`28` expected PostgreSQL-environment skips), `4` E2E tests, `99.01%` combined coverage, and dependency audit with no known vulnerabilities. PR `#423` merged by rebase to exact-main SHA `d6f9ad69`; Main Releasability `29337248995` and CodeQL `29337239463` passed, and wiki publication `23feff9` has zero drift. | Design modularity only; no runtime split is justified without workload, failure-isolation, ownership, or operability evidence. README, migrations, OpenAPI shape, database ownership, supported features, and product support remain unchanged. Issue #422 is closed on exact main with `status/merged-main`; the implementation branch is absent locally and remotely. Delete this closure branch after merge. Campaign #393 remains open. |
| `LI-CR-0053` | Outbox broker proof evidence class and package ownership | `Hardened on main` | Issue `#419` found that the proof scanned integration-test source fragments for an accepting in-process fake publisher, described that as configured-publisher runtime proof, and cleared external broker configuration/runtime blockers without contacting a broker. Flat builder, script, and test paths also obscured capability ownership. | Replace the proof with a v2 `source_contract` artifact that clears no blocker, preserves `outbox_broker_not_configured` and `external_broker_runtime_proof_missing`, rejects claim inflation, uses AST-backed publisher port/adapter contract checks, and groups the builder, generator, gate, and tests under capability-owned `outbox/broker/` packages with retired paths prohibited. | Focused proof/readiness and API coverage passed (`147` tests in the broad focused suite; `74` in the final API/type-boundary confirmation). `make check` passed MyPy over `817` files and `3,794` unit tests. `make ci` passed `451` integration tests (`28` expected PostgreSQL-environment skips), `4` E2E tests, `99.01%` combined coverage, and dependency audit with no known vulnerabilities. PR `#420` merged by rebase to exact-main SHA `53d714fc`; Main Releasability `29333295553` and CodeQL `29333289244` passed, and wiki publication `82c7433` has zero drift. | Design modularity only; no runtime split is justified. README, migrations, OpenAPI shape, database ownership, supported features, and product support remain unchanged. Issue #419 is closed on exact main; the implementation branch is absent locally and remotely. Delete this closure branch after merge. Campaign #393 remains open. Existing evidence-classification skill guidance is sufficient; repository validation and context carry the durable recurrence guard. |
| `LI-CR-0052` | Operator-workflows operations evidence class and package ownership | `Hardened on main` | Issue `#412` found that static Grafana, Prometheus, runbook, fixture, and validator files were described as certified and cleared dashboard/alert runtime blockers without provisioning, query execution, rule loading/evaluation/delivery, deployment, or production evidence. Flat builder, script, and test files also obscured capability ownership. | PR `#417` replaced the proof with a v2 `source_contract` artifact that clears no blocker, preserved explicit dashboard and alert runtime blockers, rejected claim inflation, and grouped builder, generator, gate, and tests under `operator_workflows_operations/` with retired paths prohibited. | Focused tests passed (`71`); `make check` passed MyPy over `814` source files and `3,772` unit tests. `make ci` passed `451` integration tests (`28` PostgreSQL-dependent skips), `4` E2E tests, `99.01%` combined coverage, and dependency audit with no known vulnerabilities. Main Releasability run `29329446874` and CodeQL run `29329442570` passed on exact merged SHA `a65a7f915e6f1267fc89f49ac95b5f68180fb824`; wiki publication `b01cb61` has zero drift. | Design modularity only; no runtime split is justified. README, migrations, OpenAPI shape, database ownership, supported features, and product support remain unchanged. Issue #412 is closed with `status/merged-main`; the patch-equivalent implementation branch is absent locally and remotely. Delete this closure branch after merge. |
| `LI-CR-0051` | Lifecycle and downstream-claim transaction serialization | `Hardened on main` | Exact-main Main Releasability after PR `#413` exposed issue `#414`: erasure could read an unlocked active lifecycle control and zero deliveries, then wait at update time while a concurrent downstream claim committed, allowing both operations to succeed. | Lock candidate then lifecycle control before evaluating active outbox and downstream posture; preserve non-locking replay reads; add a deterministic query-order contract test; retain the real PostgreSQL concurrency invariant. | Focused unit `16 passed`, disposable PostgreSQL 18 lifecycle `4 passed`, and `make check` with `3,763` unit tests passed. PR `#415` merged to exact-main SHA `971b3c33`; Main Releasability `29326244284` and CodeQL `29326239670` passed, including the PostgreSQL runtime lane. | Internal infrastructure-adapter modularity only. Slice 06, repository context, and lifecycle wiki source carry the durable lock-order rule; no runtime split, API, migration, or supported-feature change. |
| `LI-CR-0050` | AI model-risk operations evidence class and package ownership | `Hardened on main` | Issue `#411` found that static Grafana, Prometheus, runbook, and validator files were described as certified runtime evidence and nominally cleared two blockers that the readiness model never emitted. The flat proof modules also obscured capability ownership. | Replace the proof with a v2 `source_contract` artifact that clears no blocker, expose source-contract validity separately from runtime posture, add explicit dashboard and alert runtime blockers, reject claim inflation, and group the builder, generator, gate, and tests under `ai_model_risk_operations/`. | `165` focused tests and full local gates passed. PR `#413` merged at `3467379b`; fix-forward PR `#415` resolved exact-main defect #414. Final exact-main SHA `971b3c33` passed Main Releasability `29326244284` and CodeQL `29326239670`. | Design modularity only; the existing deployable owns the bounded policy. Provisioning, query execution, rule evaluation/delivery, deployment, production approval, and supported-feature promotion remain outside this proof. No runtime split is justified. Existing Lotus backend evidence-classification guidance identified the defect; repository validation/context were strengthened, so no additional platform skill change is warranted. |
| `LI-CR-0049` | AI lineage-store proof evidence class | `Hardened on main` | Issue `#396` found that source files and Make target text could clear a blocker that claimed certified durable lineage execution. The proof did not require a trusted execution identity or result artifact. | Add the closed `app.domain.proof_evidence` taxonomy and CI receipt, organize the builder under `app.application.ai_lineage_store_proof`, require exact-main PostgreSQL execution plus uploaded artifact digest, and keep source-contract evidence non-certifying. Exact-main run `29306713267` then exposed GitHub's bare artifact-digest form; fix-forward PR `#398` canonicalized it without weakening validation. | `121` focused proof tests, `17` disposable PostgreSQL 18 tests, full local `make ci`, PRs `#397`/`#398`, and exact-main SHA `5cf7592b` passed Main Releasability `29307190040` and CodeQL `29307186825`. The downloaded v2 proof validated, bound PostgreSQL artifact digest `sha256:6278b0ff8ec65085725fc17b4d80e01c89bc0360d68b6fce537e150b8a0afb7a`, and cleared only the lineage-store blocker. | Design modularity and CI automation only; no service split or supported-feature promotion. Issue `#393` retains the wider same-pattern inventory; platform issue `#526` tracks reusable governance. |
| `LI-CR-0048` | AI workflow runtime proof truth | `Hardened on main` | Issue `#392` found that a source-tree scanner could set `lotusAiRuntimeExecuted=true` and clear the runtime blocker without invoking Lotus AI. Synthetic files containing expected strings passed the old tests. | Replace the flat scanner with `app.application.ai_runtime_proof`, a `LotusAIWorkflowRuntime` port, and the `infrastructure.lotus_ai` HTTP adapter. Invoke the governed pack with synthetic redacted evidence; retain only a digest-bound receipt; fail closed on transport, identity, evidence, lifecycle, review, or authority mismatch. | `65` focused proof/adapter tests cover the changed boundary at `100%`; full local coverage passed at `99.03%`. A canonical Lotus AI service produced a clean-commit completed `ACTION_REQUIRED` deterministic-stub receipt. PR `#394` merged to exact-main SHA `b892d5d6`; Main Releasability `29303651841`, CodeQL `29303648849`, and wiki publication `48fd63a` passed. | Design modularity and automation only; no service split. Live-provider/production approval remains #340. Issue #393 tracks the wider evidence-classification audit. No supported-feature promotion. |
| `LI-CR-0047` | Advisor-visible AI narrative grounding | `Hardened on main` | Issue `#389` found that source-product verification covered structured claims while the accepted response still returned unrestricted provider-authored explanation text. A supported claim could therefore launder unsupported advice, guarantees, execution instructions, or client communication into advisor-visible prose. | Add a bounded `app.domain.ai_explanation` grounding policy; render accepted narrative from ordered verified claims; expose source-safe claim references; return no grounding for blocked output; reject duplicate identities; bind provider output digest and grounding policy into output integrity; keep the raw narrative out of response and persistence. | Domain and API tests prove narrative-laundering resistance, exact claim/source projection, blocked-output omission, replay conflict, and duplicate rejection. PR `#390`, exact-main SHA `67a6e005`, Main Releasability `29300549721`, CodeQL `29300546423`, and wiki publication `f86a57c` passed. | Design modularity only in the existing Idea runtime. `lotus-ai` retains provider/model/prompt/RAG ownership; live runtime, Gateway/Workbench, production model-risk approval, and supported-feature promotion remain explicit blockers. |

## Prior Entries

| Review ID | Scope / Pattern | Status | Finding | Action Taken | Evidence | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| `LI-CR-0038` | Review queue audience, authority, and module boundaries | `Hardened on main` | Issues `#385` and `#386` found audience-neutral queue/readiness behavior and caller-restated review scope. | Audience is now bound through queue policy, persistence, API, and OpenAPI; review/feedback scope derives from trusted caller entitlements plus persisted candidate truth; queue API files are grouped by capability. | PR `#387`, exact-main SHA `d5be2390`, Main Releasability `29297787754`, CodeQL `29297783153`, and wiki publication `3ad971d` passed. | Gateway/Workbench product proof, feedback mesh certification, and supported-feature promotion remain later-slice gates. |
| `LI-CR-0001` | Review workflow API route orchestration | `Hardened` | `src/app/api/review_workflow.py` duplicated caller parsing, review capability checks, caller entitlement scope validation, idempotency validation, durable-write guarding, persistence problem mapping, and operation-event emission across review-action and feedback routes. This increased design-time complexity inside a single runtime deployable. | Added `src/app/api/review_workflow_operations.py` as an API-internal bounded module for shared mutation preparation, actor-context construction, durable-write blocking, product-safe persistence problem mapping, and operation-event mapping. The route handlers now delegate shared setup while keeping domain workflow logic in `app.application.review_workflow` and runtime composition in `app.api.runtime_dependencies`. | Unit coverage in `tests/unit/test_review_workflow_api_operations.py`; existing review workflow API/application tests remain the behavior proof. Local validation for this entry must include focused review workflow tests, `make maintainability-gate`, `make architecture-boundary-gate`, and `make duplicate-implementation-gate`. | Design modularity improved only. No runtime service split: review workflow remains part of the lotus-idea API process because this slice has no workload, failure-isolation, ownership, or operability evidence for a separate deployable boundary. |
| `LI-CR-0002` | Conversion governance API route orchestration | `Hardened` | `src/app/api/conversion_governance.py` repeated conversion caller parsing, mutating capability checks, idempotency validation, durable-write guarding, persistence problem mapping, and operation-event emission across conversion-intent and conversion-outcome routes. The route also carried route metadata and response DTOs, so the orchestration made the API surface harder to review without changing runtime topology. | Added `src/app/api/conversion_governance_operations.py` as an API-internal bounded module for conversion mutation preparation, durable-write blocking, product-safe persistence problem mapping, and operation-event mapping. Added `src/app/api/operation_events.py` so review and conversion operation modules share one foundation-event helper instead of duplicating wrapper bodies. The route handlers now delegate shared setup while keeping conversion domain rules in `app.domain`, workflow orchestration in `app.application.conversion_workflow`, and runtime composition in `app.api.runtime_dependencies`. | Unit coverage in `tests/unit/test_conversion_governance_api_operations.py`; existing conversion domain and API integration tests remain the behavior proof. Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_api_error_mappings.py tests\unit\test_conversion_governance_api_operations.py tests\unit\test_review_workflow_api_operations.py tests\unit\test_conversion_governance.py tests\integration\test_review_workflow_api.py -q` (`49 passed`). Aggregate validation passed with `make lint`, `make typecheck`, `make duplicate-implementation-gate`, and `make test-unit` (`2376 passed`). Wiki check-only failed only on expected published-wiki drift for unmerged branch pages. | Design modularity improved only. No runtime service split: conversion governance remains part of the lotus-idea API process because conversion intent/outcome posture shares the same repository, audit, idempotency, and operation-event boundary and there is no workload, failure-isolation, ownership, security, or operability evidence for a separate deployable boundary. Repo context, README, wiki source, scorecard, and refactor decisions were updated; no platform skill change is justified because existing Lotus backend/codebase-review skills already cover the pattern. |
| `LI-CR-0003` | Domain persistence model boundary | `Hardened` | `src/app/domain/persistence.py` mixed immutable persistence records/results/snapshots with repository behavior and was within 15 lines of the source-file maintainability ceiling. That made persistence behavior harder to review without adding runtime value. | Added `src/app/domain/persistence_models.py` for persistence decisions, records, results, lifecycle history, and snapshots. `src/app/domain/persistence.py` now imports and re-exports those types while keeping `InMemoryIdeaRepository` behavior and the public import surface stable. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_idea_persistence.py tests\unit\test_postgres_repository.py tests\unit\test_repository_port_boundary.py tests\unit\test_domain_validation.py -q` (`46 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: persistence models and repository behavior remain inside the same domain-service deployable because there is no workload, failure-isolation, ownership, or operability evidence for a separate runtime boundary. |
| `LI-CR-0004` | Signal evaluation model boundary | `Hardened` | `src/app/domain/signal_evaluation.py` mixed immutable signal inputs, policies, outcomes, and result contracts with deterministic evaluator algorithms and stable identity builders. The file was large enough to slow review of source-authority and private-banking boundary changes. | Added `src/app/domain/signal_evaluation_models.py` for signal-family input, policy, outcome, and result contracts. `src/app/domain/signal_evaluation.py` now imports and re-exports those types while retaining deterministic evaluator algorithms and the existing public import surface. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_high_cash_signal_evaluation.py tests\unit\test_concentration_risk_signal_evaluation.py tests\unit\test_underperformance_signal_evaluation.py tests\unit\test_mandate_health_signal_evaluation.py tests\unit\test_high_volatility_signal_evaluation.py tests\unit\test_drawdown_review_signal_evaluation.py tests\unit\test_api_signal_models.py -q` (`90 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: signal evaluation remains inside the lotus-idea domain-service deployable because the evaluators consume caller/source-owned evidence, produce local candidate posture, and have no workload, failure-isolation, ownership, or operability evidence for a separate runtime boundary. |
| `LI-CR-0005` | AI governance API model boundary | `Hardened` | `src/app/api/ai_governance.py` mixed AI explanation request/response DTOs and domain mapping with route authorization, idempotency, durable-write checks, operation events, OpenAPI metadata, and response handling. That made AI governance review harder without changing runtime behavior. | Added `src/app/api/ai_governance_models.py` for AI explanation request/response DTOs and conversion helpers. `src/app/api/ai_governance.py` now imports and re-exports those DTOs while retaining route behavior, capability checks, persistence orchestration, operation events, and route metadata. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_ai_governance.py tests\unit\test_ai_governance_api_contract.py tests\unit\test_ai_explanation_readiness.py -q` (`23 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: AI explanation governance remains inside the lotus-idea API process and still does not execute AI runtime workflows, own prompts/provider payloads, or grant downstream authority. |
| `LI-CR-0006` | Outbox delivery API model boundary | `Hardened` | `src/app/api/outbox/delivery.py` mixed readiness/run-once response DTOs with route authorization, idempotency, durable-write checks, broker cleanup, operation events, OpenAPI metadata, and response handling. The route file remained a design-time review hotspot even though the runtime boundary was correct. | Added `src/app/api/outbox/delivery_models.py` for outbox delivery readiness, status-count, and run-once response DTOs. `src/app/api/outbox/delivery.py` now imports those DTOs while retaining caller checks, route behavior, publisher cleanup, operation events, and route metadata. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\integration\test_outbox_delivery_readiness_api.py tests\unit\test_outbox_delivery_readiness.py -q` (`19 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: outbox delivery remains an internal operator foundation inside the lotus-idea API process because there is no workload, failure-isolation, ownership, security, or operability evidence for a separate delivery service boundary. |
| `LI-CR-0007` | Runtime trust telemetry API model boundary | `Hardened` | `src/app/api/runtime_trust_telemetry.py` mixed preview/snapshot response DTOs and trust-telemetry payload projection with operator authorization, query validation, operation events, OpenAPI metadata, and response handling. That made source-safe mesh-readiness review harder without changing runtime behavior. | Added `src/app/api/runtime_trust_telemetry_models.py` for runtime trust telemetry preview, product posture, snapshot, freshness, lineage, blocking, and evidence response DTOs. `src/app/api/runtime_trust_telemetry.py` now imports those DTOs while retaining caller checks, timezone validation, aggregate snapshot construction, operation events, and route metadata. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\integration\test_runtime_trust_telemetry_api.py tests\unit\test_runtime_trust_telemetry.py -q` (`16 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: runtime trust telemetry remains an internal operator/data-mesh readiness surface inside the lotus-idea API process and still does not certify data products, platform mesh, Gateway/Workbench support, or supported-feature promotion. |
| `LI-CR-0008` | Idea signal API model boundary | `Hardened` | `src/app/api/idea_signals.py` mixed high-cash and mandate-restriction request/response DTOs plus command conversion with route authorization, source-ref authority checks, idempotency, persistence orchestration, operation events, OpenAPI metadata, and response handling. | Added `src/app/api/idea_signal_models.py` for route-specific idea-signal request/response DTOs and command conversion. `src/app/api/idea_signals.py` now imports those DTOs while retaining caller checks, source-authority validation, persistence orchestration, operation events, route metadata, and response handling. The operation-event integration helper now patches review/conversion helper emitter aliases so tests follow API helper extraction boundaries. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\integration\test_api_operation_events.py tests\unit\test_api_signal_models.py -q` (`24 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: idea-signal evaluation remains inside the lotus-idea API process and still only consumes caller-supplied source-owned evidence without calculating official portfolio facts, suitability, risk, performance, execution, or report authority. |
| `LI-CR-0009` | Conversion governance API model boundary | `Hardened` | `src/app/api/conversion_governance.py` mixed conversion-intent/outcome request/response DTOs and command conversion with route authorization, idempotency, conversion workflow persistence, operation events, OpenAPI metadata, and response handling. | Added `src/app/api/conversion_governance_models.py` for conversion-intent/outcome request/response DTOs and command conversion. `src/app/api/conversion_governance.py` now imports those DTOs while retaining caller checks, idempotency, conversion workflow persistence, operation events, route metadata, and response handling. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_conversion_governance.py tests\unit\test_conversion_governance_api_operations.py tests\unit\test_api_request_validation.py tests\integration\test_api_operation_events.py -q` (`37 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: conversion governance remains an internal lifecycle-intent/outcome API foundation inside the lotus-idea process and still does not grant Advise, Manage, Report, suitability, execution, render, archive, or client-communication authority. |
| `LI-CR-0010` | Review workflow API model boundary | `Hardened` | `src/app/api/review_workflow.py` mixed review-action and feedback request/response DTOs plus command conversion with caller authorization, entitlement-scope validation, idempotency, review workflow persistence, operation events, OpenAPI metadata, and response handling. | Added `src/app/api/review_workflow_models.py` for review-action/feedback request/response DTOs and command conversion. `src/app/api/review_workflow.py` now imports and explicitly re-exports those DTOs while retaining caller checks, entitlement validation, idempotency, review workflow persistence, operation events, route metadata, and response handling. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_review_workflow_api_operations.py tests\unit\test_api_request_validation.py tests\integration\test_api_operation_events.py -q` (`26 passed`), plus targeted ruff and mypy over the changed modules. | Design modularity improved only. No runtime service split: review workflow remains an internal human-review API foundation inside the lotus-idea process and still does not approve suitability, compliance, mandates, execution, reporting, client communication, or supported-feature promotion. |
| `LI-CR-0011` | Candidate detail API model boundary | `Hardened` | `src/app/api/candidate_detail.py` mixed candidate, redacted source-ref, evidence, lifecycle, review, feedback, conversion, report evidence-pack, and audit-summary response DTOs with route authorization, entitlement-scope checks, persistence lookup, operation events, OpenAPI metadata, and response handling. | Added `src/app/api/candidate_detail_models.py` for source-safe candidate-detail response DTOs and mapping. `src/app/api/candidate_detail.py` now imports and explicitly re-exports those DTOs while retaining caller checks, entitlement filtering, candidate lookup, operation events, route metadata, and product-safe response handling. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_candidate_detail_models.py tests\unit\test_candidate_detail_application.py tests\integration\test_candidate_detail_api.py tests\integration\test_api_operation_events.py::test_candidate_detail_api_emits_bounded_operation_event -q` (`12 passed`), plus targeted ruff and mypy over changed modules. Same-pattern scan reviewed large API DTO hotspots and selected candidate detail after prior review/conversion/signal/AI/outbox/telemetry model extractions. | Design modularity improved only. No runtime service split: candidate detail remains a bounded read-only API foundation inside the lotus-idea process and still does not expose raw source routes, source content hashes, portfolio accounting, suitability, execution, report rendering/archive, client communication, Workbench product support, data-product certification, or supported-feature promotion. |
| `LI-CR-0012` | Source-ingestion readiness API model boundary | `Hardened` | `src/app/api/source_ingestion_readiness.py` mixed source-ingestion readiness and run-once response DTOs with operator authorization, durable-repository checks, runtime construction, Core runtime cleanup, operation events, OpenAPI metadata, and response handling. That kept a modern internal operator proof surface harder to review without changing runtime topology. | Added `src/app/api/source_ingestion_readiness_models.py` for source-ingestion readiness and aggregate run-once response DTOs. `src/app/api/source_ingestion_readiness.py` now imports and explicitly re-exports those DTOs while retaining caller checks, durable-repository gating, runtime composition through `app.api.runtime_dependencies`, source-safe cleanup behavior, operation events, route metadata, and product-safe response handling. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_source_ingestion_readiness_models.py tests\unit\test_source_ingestion_readiness.py tests\integration\test_source_ingestion_readiness_api.py` (`28 passed`), plus targeted ruff and mypy over changed modules. Same-pattern scan continued through large API DTO hotspots after candidate detail and selected source ingestion as the next API-facing operator proof boundary. | Design modularity improved only. No runtime service split: source ingestion remains a bounded internal operator proof foundation inside the lotus-idea process and still does not certify live Core ingestion, create a data-product/source-worker service, expose portfolio identifiers, create Gateway/Workbench support, or promote a supported feature. |
| `LI-CR-0013` | Review queue API model boundary | `Hardened` | `src/app/api/review_queues.py` mixed advisor queue and queue-readiness response DTOs with caller authorization, entitlement-scope narrowing, repository selection, readiness diagnostics, operation events, OpenAPI metadata, and response handling. That made the largest remaining API route file harder to review while runtime ownership was already correct. | Added `src/app/api/review_queue_models.py` for source-safe advisor queue and review queue readiness response DTOs. `src/app/api/review_queues.py` now imports and explicitly re-exports those DTOs while retaining caller checks, entitlement filtering, repository-side projection usage, readiness snapshot construction, operation events, route metadata, and product-safe response handling. | Focused validation passed with `.venv\Scripts\python.exe -m pytest tests\unit\test_review_queue_models.py tests\unit\test_review_queue_application.py tests\integration\test_review_queue_api.py` (`37 passed`), plus targeted ruff and mypy over changed modules. Same-pattern scan continued through large API DTO hotspots and selected review queues as the largest remaining API route with route-local response DTOs. | Design modularity improved only. No runtime service split: advisor review queues remain bounded internal API/readiness foundations inside the lotus-idea process and still do not create a queue service, PM/compliance queue, Workbench product proof, data-product certification, client-ready publication, or supported-feature promotion. |
| `LI-CR-0014` | OpenAPI ProblemDetails media contract | `Hardened` | GitHub issue `#308` showed downstream submission runtime `ProblemDetails` codes were correct but generated OpenAPI only exposed examples under `application/json`; signal and high-cash persist routes had adjacent route-family metadata that could repeat the same media-type drift. | Added `application/problem+json` publication to shared `app.api.problem_details` metadata, moved signal-family and high-cash persist error metadata onto shared constructors, and extended `scripts/openapi_problem_details_example_gate.py` plus unit tests to require product-safe examples for both media types. | Focused validation passed with `.venv\Scripts\pytest.exe tests/unit/test_api_problem_details.py tests/unit/test_signal_api_contract_gate.py -q` (`21 passed`) and `.venv\Scripts\python.exe scripts/openapi_problem_details_example_gate.py` (`OpenAPI ProblemDetails example gate passed`). | Design modularity improved only. No runtime service split: product-safe API error metadata remains inside the lotus-idea API process and does not add Gateway/Workbench mutation support, client-ready publication, downstream execution authority, data-product certification, or supported-feature promotion. |
| `LI-CR-0015` | GitHub issue closure control and context hygiene | `Hardened` | The active RFC-0002 branch had implementation-backed fixes for the open actionable issue set, but the closure evidence was distributed across RFC text, repository context, tests, wiki, and local memory. That increased design-time complexity and made it easy to overclaim "all issues fixed locally" without one governed evidence surface. | Added `docs/architecture/GITHUB-ISSUE-CLOSURE-MATRIX.md` as the concise issue closure index for `#301`-`#303` and `#306`-`#320`, added `scripts/github_issue_closure_matrix_gate.py`, wired `make github-issue-closure-matrix-gate` into `make lint` and `make ci-contract-gate`, and updated README/RFC/wiki/context to point to the matrix instead of repeating long per-issue evidence. | Focused validation must include `make github-issue-closure-matrix-gate`, `make ci-contract-gate`, `make documentation-contract-gate`, and `tests/unit/test_github_issue_closure_matrix_gate.py`. | Design modularity and governance clarity improved only. No runtime service split: issue closure is a repository delivery control and does not alter lotus-idea runtime ownership, API behavior, supported-feature posture, Gateway/Workbench proof, or data-product certification. |
| `LI-CR-0016` | Caller-supplied signal route orchestration | `Hardened` | The caller-supplied signal routes repeated entitlement and access-scope checks, source-ref contract validation, application evaluator invocation, operation-event emission, and response projection across Core, Risk, Performance, Advise, and Manage signal families. The duplication increased review surface and made ordering regressions possible when a new source family was added. | Added typed `evaluate_caller_supplied_signal` orchestration in `src/app/api/signal_api_support.py` and routed all caller-supplied signal families through it. The helper supports both all-contract and one-of-contract validation while leaving source-backed runtime construction and cleanup in the route-specific path. | Focused validation passed with `python -m pytest tests/unit/test_signal_api_support.py tests/integration/test_high_cash_signal_api.py tests/integration/test_mandate_restriction_signal_api.py tests/integration/test_concentration_risk_signal_api.py tests/integration/test_underperformance_signal_api.py -q` (`76 passed`), plus the full caller-supplied signal integration pack (`161 passed`), `ruff`, `mypy`, and the signal API contract gate. | Design modularity improved only. No runtime service split: all signal evaluators remain in the existing `lotus-idea` deployable because they share entitlement, source-contract, audit-event, and domain policy boundaries and there is no workload, failure-isolation, ownership, security, or operability evidence for a separately scalable signal service. |
| `LI-CR-0017` | Advisor queue temporal paging | `Hardened` | GitHub issue `#332` showed `evaluatedAtUtc` affected snooze evaluation but not candidate visibility, while offset pages had no state identity. Future-created candidates could enter historical reads and concurrent inserts or mutations could silently shift later pages. | Added pure `app.domain.review_queue.snapshot` policy, explicit temporal/snapshot repository-port fields, inclusive candidate-created-at filtering, opaque scope/policy/state-bound identity, required continuation tokens, stable 400/409 errors, PostgreSQL before/after fingerprint verification, and a cohesive PostgreSQL queue mixin. Added a blocking cross-layer contract gate. | Exact-boundary, source-date, missing/malformed/stale-token, future-insert, fake-adapter, API, OpenAPI, lint/typecheck, and real PostgreSQL runtime proof pass. | Design modularity improved only. No runtime service split: queue policy and persistence remain inside lotus-idea; Workbench proof, data-product certification, PM/compliance queues, client publication, and supported-feature promotion remain blocked. |
| `LI-CR-0018` | Multi-source temporal reconciliation | `Hardened` | Issue `#333` had a shared exact-date helper, but allocation-drift checked only its required Manage ref before including optional Performance/Risk mandate-health refs. Temporal policy was also implicit rather than declared per family, and correction identity behavior was undocumented. | Added versioned all-family temporal contracts, checked every included allocation-drift source for temporal/freshness posture, retained shared caller/adapter/ingestion enforcement, and declared content-hash correction as new lineage-bound candidate identity. Extended the static gate. | Exact-boundary, mismatch, future, stale, multi-source conflict, correction identity, source-backed API, source-ingestion no-persistence, all-family declaration, lint, and typecheck proof. | Design modularity improved only. No runtime split: reconciliation stays a pure domain policy shared by existing signal use cases; source owners retain facts and official calculations. |
| `LI-CR-0019` | Trusted Core tenant propagation | `Hardened` | Issue `#335` exposed a tenant-aware Core snapshot fallback. Earlier local fixes carried tenant into Core request DTOs, but successful Core-backed candidates still used `tenant_id=unknown`, generated ingestion keys were not tenant-bound, unknown request fields were silently ignored, and operation events had no bounded scope-provenance posture. | Added a shared tenant-plus-portfolio access-scope constructor; applied it to high-cash, low-income, bond-maturity, and missing-benchmark Core paths; tenant-bound generated ingestion identities; rejected unknown API fields; emitted bounded tenant-scope provenance; forbade raw tenant operation attributes; and added `trusted-tenant-context-gate`. | Tenant A/B adapter payload, candidate identity, access-scope, and ingestion isolation; missing, ambiguous, untrusted-header, and body-override rejection; all four application families; OpenAPI; focused tests; lint; and typecheck. | Design modularity improved only. No runtime split: trusted scope resolution remains in the shared API/application boundary and existing Core adapter because no workload, failure-isolation, ownership, or operability evidence justifies another service. Core remains source authority and receives tenant only where its published route contract supports it. |
| `LI-CR-0020` | Caller-context ProblemDetails boundary | `Hardened` | Issue `#336` was partially addressed by typed caller-context exceptions, but runtime media still defaulted to `application/json`, generated OpenAPI did not guarantee dependency-level 400/403 examples on every protected operation, diagnostic categories were generic, route-local safe details drifted, and no representative cross-route test bound the layers together. | Preserved exact typed status/code/title/detail/category through the global handler; standardized safe caller-scope details; served runtime failures as `application/problem+json`; injected both caller-boundary examples for every protected operation; added bounded diagnostic categories; and expanded `caller-context-contract-gate` across exception, handler, OpenAPI, media, route registry, and tests. | Representative signal, lifecycle, review, AI, report, downstream, and readiness integration coverage proves exact problems, sanitized correlation, source-safe logs, and no raw token/header/scope leakage. Focused ruff, mypy, caller-context, OpenAPI, endpoint-certification, and ProblemDetails gates pass before full repository validation. | Design modularity improved only. No runtime split: caller-context handling remains a stable shared API boundary in the existing deployable. Existing backend-governance Rule 18 already captures the reusable cross-layer lesson, so no duplicate skill change is warranted. |
| `LI-CR-0021` | Exact outbox recovery lookup and real PostgreSQL claim proof | `Hardened` | Issue `#337` had broad recovery implementation, but PostgreSQL resolved opaque support references by scanning and locking only the latest 1,000 outbox rows. Older dead letters could become unreachable. The first real recovery scenario also exposed ambiguous unqualified `RETURNING` columns in the shared delivery `UPDATE ... FROM` claim. | Replaced the bounded scan with a state-aware exact SHA-256 selector backed by an immutable expression index; qualified shared delivery returning columns from one structured column vocabulary; extended migration and recovery gates; and added fixed-window mutation protection. | Unit tests prove more than 1,000 unrelated newer rows cannot hide a dead letter. Required real PostgreSQL proof covers migration apply/rollback, delivery claim, dead-letter transition, connection reload, exact recovery claim, durable audit replay, and queue behavior (`11 passed`). | Design modularity improved only. No runtime split: recovery stays inside the existing persistence adapter and application use case. The repository gate captures the concrete regression; the reusable backend skill lesson is evaluated separately to avoid duplicating local SQL detail in prose. |
| `LI-CR-0022` | Service capacity contract/runtime drift and source-safe baseline evidence | `Hardened` | Issue `#345` exposed service SLO and capacity gaps. The same-pattern scan found request, dependency-pool, timeout, retry, batch, and PostgreSQL threshold budgets duplicated or absent across runtime and contract, while unconstrained evidence could retain caller/business data or overclaim test results. | Centralized code-owned limits; made gates compare contract/runtime values; added aggregate evidence, probe ports/adapters, guarded workload orchestration, shared PostgreSQL capacity projection, bounded metrics/alerts, and fail-closed nonessential source/outbox shedding. | Ruff, mypy, contract gates, `promtool`, source-safe adapter, failure/recovery, PostgreSQL projection, exact-threshold, mutation-safety, and claim tests pass. Full final CI and qualifying production-like load/soak, threshold stress/recovery, and cost/resource evidence remain pending. | Design modularity improved only. No runtime split: policy, projection, probes, metrics, and operator automation remain bounded internal modules because measured evidence does not justify another deployable. Test evidence, observed utilization, and implemented shedding remain distinct from production capacity certification. |
| `LI-CR-0023` | Stale PostgreSQL statistics and caller-asserted evidence can bypass capacity controls | `Fixed locally` | Controlled testing exposed transaction-cached utilization. The same-pattern review found unverified saturation and cost/resource booleans plus a self-declared `production-like` profile could clear blockers. | Refresh session statistics; remove asserted evidence inputs; add layered threshold automation; require a protected main-only producer workflow and GitHub attestation verification pinned to repository, signer, ref, and commit. | Focused unit, typing, lint, contract, workflow-shape, disposable PostgreSQL threshold/recovery, and aggregate coverage checks pass. Production-like execution remains blocked until merge and protected runtime configuration. | Test proof cannot certify. Serialized verification claims cannot replace the typed verifier receipt. No runtime split was introduced. |
| `LI-CR-0024` | Process telemetry could be mistaken for cost or scale certification | `Hardened locally` | Issue `#345` required cost/resource evidence, but no governed collector existed and a prior boolean could assert completion. A same-pattern review showed that storing raw Prometheus scrapes or endpoint identity would also expand the evidence sensitivity boundary. | Add a bounded snapshot model, narrow port, official Prometheus parser adapter, 1 MiB response ceiling, guarded one-hour collection ceiling, source-safe aggregate artifact, and blocking contract gate. | Focused model, adapter, CLI, source-safety, claim-inflation, HTTP-failure, cardinality, live local `/metrics`, and aggregate coverage checks pass. | Process telemetry remains distinct from cost attribution, horizontal scale, and runtime-split evidence. Production-like resource qualification is governed separately in `LI-CR-0030`. |
| `LI-CR-0025` | Boolean dependency-fault expectation could accept unrelated blocked outcomes | `Fixed locally` | Issue `#345` dependency-failure workload treated any blocked run as an expected fault, while the HTTP probe discarded typed source-failure evidence and OpenAPI exposed only one of two 502 dependency codes. Entitlement, configuration, capacity, or mixed failures could therefore produce false recovery evidence. | Replace the boolean with closed `source_unavailable` qualification; expose bounded aggregate source-failure counts; retain only governed dependency codes; require exclusive intended-fault evidence and completed/replayed recovery with zero failures; publish both 502 contracts. | Cross-layer domain, API, OpenAPI, probe, workload, and runner tests pass. Exact aggregate evidence: lint/contract gates pass, MyPy passes 636 files, and coverage passes at 99.01% with 3,088 unit, 408 integration passed/17 skipped, and 4 E2E tests. | This is design modularity within the existing Idea service. Controlled production-like fault injection and attested recovery remain required; no supported feature or runtime split is justified. |
| `LI-CR-0026` | Local recovery samples could falsely clear an attestation blocker | `Fixed locally` | Issue `#345` required attested controlled recovery, but the aggregate model removed its dependency blocker whenever any local measurement carried `recovered=true`. This made observed behavior indistinguishable from protected production-like qualification. | Separate observed and attested recovery posture; add closed qualification policy, dedicated signer verification, exact-commit binding, protected manual main-only producer workflow, source-safe artifact validation, and consumer CLI verification. | Qualification, signer, baseline, CLI, workflow-shape, CI-contract, SLO-contract, and baseline-contract tests pass. Exact aggregate evidence: lint/contract gates pass, MyPy passes 636 files, and coverage passes at 99.01% with 3,109 unit, 408 integration passed/17 skipped, and 4 E2E tests. | The workflow must merge and execute successfully in the protected environment before the blocker clears. Load/soak, saturation, resource/cost, runtime split, and supported-feature claims remain separate. |
| `LI-CR-0027` | Downstream capacity was claimed without an executable scenario | `Fixed locally` | Issue `#345` and the SLO runbook required downstream load/failure evidence, but the closed baseline vocabulary and runner covered only API, source ingestion, outbox, dependency failure, and PostgreSQL. | Add contract-gated `downstream_submission` evidence, restrict it to pre-seeded synthetic Idea handoff routes, require governed capability and unique transient idempotency, accept only successful responses, and keep resource identity out of artifacts. | Focused baseline, runner, path-rejection, contract-drift, maintainability, Ruff, and MyPy checks pass. Exact aggregate evidence: lint/contract gates pass, MyPy passes 636 files, and coverage passes at 99.01% with 3,116 unit, 408 integration passed/17 skipped, and 4 E2E tests. | Canonical front-office automation still needs a governed synthetic conversion-intent seed before protected load/soak execution. This measures handoff only and introduces no downstream authority or runtime split. |
| `LI-CR-0028` | Downstream capacity depended on an ungoverned pre-seeded identifier | `Fixed locally` | The new downstream scenario correctly rejected arbitrary paths but had no deterministic way to construct its synthetic conversion intent, encouraging manual IDs or accidental client-resource discovery. | Add layered deterministic seed orchestration, bounded public-API adapter, explicit confirmation, synthetic source/scope namespace, replay-stable mutations, atomic non-certifying manifest, exact workload binding, Make target, and contract enforcement. | Focused application, adapter, CLI, manifest, tamper, response-boundary, lifecycle-order, replay-identity, Make/contract, lint, maintainability, and MyPy checks pass. Exact aggregate evidence: lint/contract gates pass, MyPy passes 642 files, and coverage passes at 99.02% with 3,137 unit, 408 integration passed/17 skipped, and 4 E2E tests. | Idea-local seed automation is implemented. Canonical platform/Workbench invocation and live protected execution remain required; no direct database seed, business-source authority, supported feature, or runtime split is introduced. |
| `LI-CR-0029` | Elapsed workflow time could misrepresent a short traffic burst as sustained load/soak evidence | `Fixed locally` | Load qualification checked only top-level elapsed time, so a burst followed by idle waiting could satisfy the one-hour window without sustained scenario observation. The same-pattern review also found duplicated SLO thresholds, stale blocker names, and no artifact-specific pre-attestation gate. | Add monotonic sample offsets and per-scenario span qualification; cycle all five steady-state scenarios through shared paced rounds; require equal volume/concurrency and PostgreSQL parity; add a protected main-only producer, synthetic seed step, exact artifact gate before attestation, and contract/runtime drift enforcement. | Deterministic scheduler, short-span rejection, scenario completeness, parity, artifact-gate, workflow-shape, and SLO-drift tests pass. Aggregate validation passes with `make lint`, MyPy over 643 source files, and 99.02% coverage from 3,168 unit tests, 408 integration tests passed/17 skipped, and 4 E2E tests. | Design modularity improved inside the existing service. Evidence orchestration reuses application ports and infrastructure adapters; dependency recovery remains separate, no production certification or supported feature is claimed, and no runtime split is justified. |
| `LI-CR-0030` | Idle or caller-inflated process telemetry could be mistaken for qualified resource and cost evidence | `Fixed locally` | The raw resource validator did not validate quantitative fields, accepted only a test profile, and the aggregate used one combined cost/resource boolean. A future attestation of an idle or tampered artifact could not truthfully establish resource posture, while clearing a combined blocker would overclaim platform billing authority. | Reject non-finite/tampered measurements; use explicit report-only resource and cost fields; require 61 samples spanning one hour concurrently with paced load; fail fast across both producers; gate and attest artifacts separately; require exact signer verification; split resource and cost blockers; retain cost ownership in `lotus-platform#495`. | Focused model, qualification, aggregate, CLI, artifact-gate, workflow-shape, YAML, and contract checks pass. Aggregate validation passes with `make lint`, MyPy over 645 source files, and 99.02% coverage from 3,192 unit tests, 408 integration tests passed/17 skipped, and 4 E2E tests. | Design modularity improved inside the existing service. Resource observation reuses existing ports/adapters and does not justify a runtime split. Idea can qualify process-resource observation but cannot own official cost allocation, billing reconciliation, or provider evidence. |
| `LI-CR-0031` | Allowed AI action enums could carry unsafe provider-authored directives | `Fixed locally` | Issue `#338` showed that the verifier trusted free-form labels whenever the action enum was allowed. Execution, approval, recommendation-publication, or client-contact instructions could therefore hide behind advisor-review or missing-evidence types and enter responses, lineage, and audit. | Add a versioned deterministic domain policy before claim verification; normalize adversarial content; fail closed on forbidden or ambiguous labels; replace accepted labels with server-owned canonical wording; persist only bounded reason and policy-version lineage; bind readiness and model-risk contracts to code-owned policy truth. | Adversarial policy, domain, API replay/conflict, persistence-codec, shared contract-validator, model-risk contract, endpoint-certification, and OpenAPI tests/gates pass. Aggregate validation passes with MyPy over 647 source files, 3,231 unit tests, 409 integration tests passed/17 skipped, 4 E2E tests, and 99.02% coverage. | This is design modularity inside the existing Idea process. `lotus-ai` continues to own prompts, provider execution, RAG, and runtime controls; no service split or supported-feature promotion is justified. |
| `LI-CR-0032` | AI lineage hashes omitted advisor-visible output content | `Fixed locally` | Issue `#339` proved that changed explanation or claim text could share a lineage hash when IDs and action types remained constant, preventing reliable replay conflict and content-level audit evidence. | Add an internal versioned canonical integrity module; commit ordered explanation, claim, action, workflow/evaluator, and policy content; persist digest/version only; include both in lineage hash, audit, and API; add forward migration with explicit pre-v1 unverifiable posture; verify PostgreSQL column/JSON/hash parity on hydration. | Adversarial content-dimension/order/canonicalization tests, accepted/blocked/fallback API replay and conflict tests, in-memory snapshot recovery, PostgreSQL codec/reload/tamper tests, migration gates, model-risk/lifecycle contract gates, endpoint certification, OpenAPI, architecture, and maintainability checks pass. Aggregate validation passes with MyPy over 651 source files, 3,239 unit tests, 412 integration tests passed/17 skipped, 4 E2E tests, and 99.01% coverage. | Design modularity improved with bounded domain integrity and infrastructure row-decoder modules. No runtime split: Idea owns deterministic output governance and lineage, while `lotus-ai` retains provider, prompt, RAG, model, and runtime authority. |
| `LI-CR-0033` | Self-asserted workflow output could appear provider-attested in production-like profiles | `Producer/consumer contract implemented; live runtime blocked` | Issue `#340` found that capability-authorized callers could submit workflow output without signed Lotus AI run/model provenance. Workflow-pack allowlisting and output integrity did not authenticate execution. | Add a versioned Idea trust policy; permit visibly unattested fixtures only in local/test; reject production-like output before lookup/write; preserve fallback; verify the producer envelope and keys; persist bounded provenance with replay fencing; and expose truthful readiness. Producer issue `lotus-ai#113` is closed. | Profile-policy, production API no-write, signed-attestation positive/negative/key-rotation, digest binding, replay, lineage/codec, migration, model-risk contract, OpenAPI, endpoint, architecture, maintainability, PostgreSQL, and fail-closed tests pass. Producer and consumer mainline evidence is recorded in the model-risk contract. Live provider execution, runtime trust, Workbench proof, and promotion remain absent. | Design modularity only inside the existing Idea process. No runtime split and no Idea ownership of signing keys, provider/model inventory, prompts/RAG, or AI execution. Issue `#340` remains open only for live production certification evidence, not missing contract implementation. |
| `LI-CR-0034` | AI metadata denylist admitted unknown provider-bound fields and value-only sensitive content | `Fixed locally` | Issue `#341` found that the generic metadata map rejected named sensitive keys but accepted unknown variants and unrestricted values, so future provider integration could forward client, account, token, or routing data under neutral names. | Replace the denylist with `lotus-idea.ai-metadata-envelope.v1`: a typed closed API DTO plus purpose-aware domain policy with closed values, field/count/length/control bounds, product-safe errors, pre-lookup rejection, versioned response/readiness posture, and a machine-checked model-risk forwarding/retention contract. | Adversarial key/value/purpose/count/length/control tests, pre-route and pre-lineage rejection tests, closed OpenAPI schema, response/readiness version tests, Ruff, MyPy, and model-risk contract gate pass. Aggregate validation passes with MyPy over 654 source files, 3,257 unit tests, 421 integration tests passed/17 skipped, 4 E2E tests, and 99.02% coverage. | Design modularity improved inside the existing Idea process. Future Lotus AI adapters may receive only the validated mapping; raw values are not persisted or logged. No provider execution, runtime split, model-risk certification, or supported-feature promotion is claimed. |
| `LI-CR-0035` | Pre-push image digest placeholder contradicted published artifact identity | `Fixed locally; mainline publication pending` | Issue `#342` found that Docker labels and `/version` claimed `registry-digest-resolved-in-release-evidence` while the final registry digest was known only after push. The image could not truthfully contain its own digest because changing the label changes the digest. | Add `lotus.image-identity.v1`; retain immutable OCI build identity; bind final digest through signed release/deployment metadata; expose explicit local/digest-bound runtime posture; fail production-like readiness closed; run the exact digest image; cross-check OCI labels, `/version`, release manifest, Kubernetes reference, signature, provenance, and SBOM subjects. | Runtime policy, readiness, endpoint, container smoke, release validator, CI mutation, Docker build, OCI-label inspection, maintainability, and CI contract tests/gates pass locally. Aggregate validation passes with MyPy over 657 source files, 3,269 unit tests, 421 integration tests passed/17 skipped, 4 E2E tests, and 99.02% coverage. Mainline publication proof remains required. | Design/runtime configuration modularity only. No new deployable, rebuild, post-push mutation, mutable-tag authority, secret build input, or supported-feature claim. Issue closes only after the merged Main Releasability workflow publishes and validates the digest evidence. |
| `LI-CR-0036` | Dependency locks and release evidence lacked enforceable license/IP authority | `Fixed locally; mainline publication pending` | Issue `#346` found exact pins and an SBOM but no complete SPDX inventory, deterministic attribution, conditional-obligation policy, exception lifecycle, or final-artifact compliance binding. | Add one versioned compliance contract, a named policy module, and a thin CLI gate; derive notices deterministically; bind policy, locks, NOTICE, SBOM, exceptions, and digest in Main Releasability; route source review while keeping legal/security approval external. | Adversarial policy/release/CI tests, wheel metadata, SHA-tagged container inspection/smoke, license and security gates, MyPy over 658 files, 3,281 unit tests, 421 integration tests passed/17 skipped, 4 E2E tests, and 99.02% aggregate coverage pass locally. Mainline release evidence remains required. | Design modularity only: compliance remains a bounded repository/release module. No runtime service split, legal self-certification, base-image rights claim, or supported-feature promotion is justified. |
| `LI-CR-0037` | Non-finite capacity measurements could bypass load shedding and corrupt evidence | `Fixed locally` | Range-only checks accepted `NaN` because comparisons with non-finite values are false. A corrupted PostgreSQL utilization sample could therefore be classified as `normal`, allow nonessential work, and propagate invalid latency, queue-age, observation-window, or utilization figures into a source-safe capacity artifact. | Require finite values at the PostgreSQL capacity-posture domain boundary and independently at the capacity-measurement and aggregate-evidence boundaries. Preserve `None` as the only unavailable-utilization representation so collection failure remains fail-closed. Add negative, `NaN`, and infinity coverage across posture and evidence construction. | Cross-layer focused capacity tests pass (`61 passed`), with targeted Ruff and MyPy clean. Full repository validation remains part of the Slice 15 commit gate. | Design hardening only inside the existing process. Capacity posture, evidence aggregation, ports, and adapters retain their current bounded modules; no workload, failure-isolation, ownership, or operability evidence justifies a separate runtime service. |
| `LI-CR-0038` | Application-owned or self-asserted cost evidence could overclaim FinOps authority | `Consumer implemented; execution pending` | Slice 15 required cost evidence, but Idea correctly lacked billing authority. A local boolean or schema-valid JSON could otherwise clear the cost blocker without authoritative allocation, reconciliation, or provenance. | Implement the producer contract, Decimal methodology, normalized export adapter, protected signer, and qualification in `lotus-platform#495`; add an Idea verifier that pins platform repository/signer/main-ref/commit/digest and binds the platform resource digest/run id to Idea's independently attested resource proof. | Platform focused tests and the 613-test feature lane pass; Idea application, aggregate, verifier, and CLI coverage pass in the focused capacity suite. Protected mainline execution remains required. | Design modularity only. Platform owns FinOps methodology and evidence production; Idea owns narrow verification and capacity-readiness binding. No billing logic or runtime service moves into Idea, and no supported feature is promoted. |
| `LI-CR-0039` | Contract-green DR automation failed under the real hosted runtime | `Logical drill fixed; production certification blocked` | Mainline execution exposed four assumptions absent from local contract tests: runner client 16 against server 18, no PostgreSQL 18 package source, subprocess diagnostics reduced to an exception type, and `pg_restore` invoked without an explicit target database. | Install and verify matching PostgreSQL 18 client/server versions from the signed PGDG source; enforce workflow shape in the CI contract; add phase-specific redacted diagnostics; pass the parsed restore target explicitly; cover string and byte diagnostics plus target selection. | PRs `#351`-`#354` passed full merge gates. Main dispatch `29157824527` at `e565c915` passed backup, 17-table restore through migration `012`, replay, fencing, proof gate, provenance, and artifact upload with RPO `0s` and RTO `0.193s`. | Design and operability hardening remain inside the existing service and workflow. No runtime split is justified. Logical proof remains `not_certified`/non-PITR; provider backup, encryption, WAL, failover, and cutover authority remain external blockers under #343. |
| `LI-CR-0040` | Lifecycle capability package and executable naming | `Hardened locally` | Lifecycle policy, signed authority, schedule, DTO, port, adapter, mapping, runtime, test, and operator-script files were spread across large flat layer/support directories. The repository also named an executable foundation-posture gate after the RFC slice that introduced it, and source-proof checks depended on incidental implementation text. | Added a `data_lifecycle/` capability package inside every existing runtime layer and `scripts`, mirrored focused tests, made runtime depend on authority protocols instead of the HTTP adapter type, renamed the foundation structure gate for its invariant, made AI attestation proof follow stable security interfaces, and extended repository hygiene to require canonical paths while rejecting retired flat paths and RFC/slice-coupled executable names. The naming rule excludes non-executable contracts and supports an explicit allowlist only for genuine RFC-tracking executable artifacts. | Focused layer migrations pass 127-182 tests per step plus Ruff, MyPy, architecture, OpenAPI, repository-hygiene, direct CLI `--help`, and `app.main`/package import proof. Final `make ci` passes with MyPy over 719 source files, 3,510 unit tests, 426 integration tests passed/18 environment-dependent skips, 4 E2E tests, 99.02% coverage over 23,173 statements, and no known dependency vulnerabilities. Reusable guidance is committed in lotus-platform `8f695d1`; issue `#357` tracks measured expansion beyond the pilot. | Design modularity only. The layered API-to-adapter dependency direction is preserved, shared cryptography/transport remain outside the feature package, and no directory-size, workload, failure-isolation, ownership, security, or operability evidence justifies a lifecycle service split. |
| `LI-CR-0041` | Outbox capability ownership remained implicit across flat layer and support directories | `Hardened locally` | After the lifecycle pilot, the outbox family remained distributed across 34 production/support modules and 22 focused test/helper modules. Generic root names such as `domain/events.py` obscured ownership, scripts duplicated prefix-based discovery, and a partial move could leave stale package truth in contracts or direct-script execution. | Added `outbox/` packages inside API, application, domain, ports, infrastructure, runtime, observability, scripts, and focused tests; moved event lineage and in-memory write behavior into the domain package; migrated all imports and source-of-truth paths atomically; added direct-script bootstrap; and extended repository hygiene to require canonical paths and reject every retired flat location. Broader implementation-proof consumers remain with their owning proof capability. | Focused outbox/domain/hygiene validation passes with 293 tests and one environment-dependent PostgreSQL skip. Final `make ci` passes with MyPy over 739 files, 3,567 unit tests, 430 integration tests passed/19 environment-dependent skips, 4 E2E tests, 99.02% coverage over 23,779 statements, and no known dependency vulnerabilities. A separate PostgreSQL 18 gate passes all 16 tests. Clean wheel contents/imports, SHA-tagged Docker build, container package imports, health/version smoke, and OCI label inspection pass. | Design modularity only. The API and optional worker remain roles of one Idea service over one Idea-owned PostgreSQL boundary. No compatibility alias, broker service, database split, external publication certification, consumer authority, platform-mesh claim, or supported-feature promotion was added. |
| `LI-CR-0042` | Candidate persistence hydrated unrelated PostgreSQL state | `Hardened on main` | Each candidate write called the generic repository snapshot loader, so ingestion cost grew with all candidates, lifecycle, audit, outbox, review, feedback, conversion, report, downstream, and AI-lineage history. Concurrent candidate writers could also reach the candidate primary key before idempotency fencing and leak a database conflict. | Added the first `infrastructure/persistence` capability module, a mutation-visible single-candidate projection, and ordered transaction-scoped candidate/idempotency advisory locks. Candidate persistence now evaluates the unchanged domain repository over only requested and idempotency-linked aggregates, then applies the existing atomic row delta. Focused tests are grouped under matching unit/integration persistence packages and CI/repository hygiene require them. | Query-shape tests prove unrelated tables are not read; real PostgreSQL 18 proves same-key accepted/replayed and different-key accepted/duplicate-candidate races with exactly one candidate/idempotency/audit/outbox side-effect set per accepted identity. PR `#365`, Main Releasability `29239140276`, and CodeQL `29239134509` are green on `69326064`. | Runtime modularity is unchanged: API and worker roles share one Idea-owned PostgreSQL database. The same-pattern family was completed in `LI-CR-0043`. No schema, OpenAPI, supported-feature, platform-skill, or source-authority change is justified. |
| `LI-CR-0043` | Ordinary mutations and replay hydrated the whole Idea store | `Hardened on main` | After candidate persistence was bounded, lifecycle, review, feedback, conversion, report evidence, AI lineage, outbox run idempotency, evidence replay, and report precheck still reused full snapshots. Their cost and coupling grew with unrelated history. | Split aggregate snapshot composition, PostgreSQL mutation orchestration, and replay projections into named persistence modules. Exact mutation identities fence sorted candidate and idempotency locks; only command, identity-linked, or idempotency-linked candidates are hydrated. AI request and attestation/provider replay nonces have bounded linkage. | Query-shape and behavior tests pass; real PostgreSQL exposed and verified the nullable replay-nonce bind fix; all 17 disposable PostgreSQL 18 tests pass. PR `#365`, Main Releasability `29239140276`, and CodeQL `29239134509` are green on `69326064`; wiki commit `8386705` has zero drift. | API and worker roles still share one Idea-owned database. Full snapshot/replace remain administrative/test/DR behavior. No schema, migration, API/OpenAPI, source authority, supported feature, platform skill, or runtime split changed. |
| `LI-CR-0044` | In-memory outbox repository ownership remained split after capability packaging | `Hardened on main` | `src/app/domain/persistence.py` still owned delivery selection and leasing, publish/failure transitions, dead-letter summaries, and operator recovery after outbox event construction had moved into the capability package. The root repository remained 1,185 lines and required outbox reviewers to cross ownership boundaries. | Replaced the write-only mixin with `InMemoryOutboxRepositoryMixin`, which owns all in-memory outbox write, delivery, lease, failure, dead-letter, and recovery behavior. The root repository now composes that stable capability interface and is 993 lines. Corrected its stale pre-database docstring and added an ownership test covering all eight methods. | Focused outbox, persistence, PostgreSQL repository, and repository-hygiene validation passes with 97 tests, plus targeted Ruff and MyPy. Aggregate validation passes with all lint and deterministic contract gates, MyPy over 750 source files, 3,576 unit tests, 430 integration tests with 20 environment-dependent PostgreSQL skips, 4 E2E tests, 99.01% coverage over 23,928 statements, OpenAPI certification, and no known dependency vulnerabilities. PR `#369`, exact-main Main Releasability run `29252433997`, and CodeQL run `29252407925` are green on `f0f06c59`; wiki drift and branch hygiene are clean. | Design modularity only. API and optional worker roles still share one Idea-owned PostgreSQL database; no schema, migration, port, adapter, API/OpenAPI, wiki, repository-context, supported-feature, source-authority, platform-skill, or runtime change is justified. External Slice 06 publication and production-certification blockers remain unchanged. |
| `LI-CR-0045` | Review and feedback persistence remained embedded in the root in-memory repository | `Hardened on main` | `src/app/domain/persistence.py` still owned review decisions, feedback events, identity conflict/replay fencing, lifecycle-history projection, audit persistence, and outbox emission. Five cohesive methods spanned about 230 lines and forced review-workflow changes through the root repository. | Added `InMemoryReviewWorkflowRepositoryMixin` in `src/app/domain/persistence_review_workflow.py`; the root repository composes the existing repository contract and is reduced from 993 to 766 lines. The review-identity contract gate follows the new authoritative path, and a method-ownership regression test prevents the behavior from drifting back into the root. The first aggregate run exposed a root-file-only outbox gate assumption; the gate now governs an explicit event-writer inventory, parses calls with Python AST, and rejects missing, duplicate, ungoverned, or lineage-free event families across bounded modules. | Focused review/persistence/application/API validation passes with 68 tests, followed by 50 tests after replacing duplicate executable stubs with typed mixin requirements. Outbox mutation tests prove extracted-writer discovery and lineage enforcement. Final `make ci` passes with MyPy over 752 source files, 3,579 unit tests, 430 integration tests passed/20 environment-dependent PostgreSQL skips, 4 E2E tests, 99.00% coverage over 23,944 statements, OpenAPI and migration certification, zero duplicate clusters, and no known dependency vulnerabilities. PR `#370`, exact-main Main Releasability run `29254803902`, and CodeQL run `29254796725` are green on `434076d6`. | Design modularity only. Review remains a human-governed Idea workflow behind the existing repository port; API and optional worker roles still share one Idea-owned PostgreSQL database. No schema, migration, port, adapter, API/OpenAPI, source-authority, wiki, repository-context, supported-feature, service, or database-boundary change is justified. |
| `LI-CR-0046` | Deployment migrations lacked serialization, durable history, and release evidence | `Hardened on main; protected execution pending` | Issue `#372` found that the local migration runner executed the complete SQL plan on every invocation with no advisory lock, applied-history checksum, pending-only selection, release/image binding, explicit legacy adoption, or source-safe deployment artifact. Concurrent deployments, edited history, and repeated data migration could not fail closed. | Added a layered deployment-migration domain contract, application use case, port, PostgreSQL adapter, exact-image CLI, pinned structural adoption contract, evidence validator, protected workflow, and blocking repository gate. PostgreSQL 18 now serializes plans, commits schema/history/events atomically, rejects bundle/history drift, applies only pending versions, requires explicit fingerprinted adoption, and database-enforces append-only migration events. The workflow verifies signed/attested mainline image identity and uses the same digest for execution and evidence validation. | Focused domain/evidence/CI suites pass with 121 tests; workflow/CI meta-contract coverage passes with 112 tests; eight disposable PostgreSQL 18 cases prove fresh apply, no-op replay, adoption, drift, atomic failure, rollback/reapply, concurrent locking, exact CLI evidence, and event mutation rejection. Full `make ci` passes with 3,611 unit tests, 430 integration tests passed/28 environment-dependent PostgreSQL skips, 4 E2E tests, 99.01% coverage over 24,237 statements, and no known dependency vulnerabilities. PR `#373` merged by rebase at `6ba9618a`; exact-main Main Releasability `29261043056` and CodeQL `29261035371` passed. Release evidence binds digest `sha256:24cff4c4528f6d06eba2f87e2d151f104e6e2303785ccfcfef589511f0ad46b7` to the SHA tag, OCI labels, `/version`, zero-finding image/secret scan, SBOM, Cosign signature, provenance, Kubernetes digest reference, and same-image promotion policy. Wiki publication `bda1965` has zero source drift. Protected execution remains pending. | Design and operability modularity only. API and optional worker roles still share one Idea-owned PostgreSQL database. Local/disposable `make migrate` use is separated from deployment authority, and static scanning rejects future workflow bypass except two governed disposable fixtures. No migration service, database split, production certification, or supported-feature promotion is justified. |
